# Floe 1.7 本轮继续实施状态

本文件记录开发事实，不把源代码、安装、编译或组件测试等同于完整功能验收。
平台顺序：iPadOS 27 优先，其次 iOS 27；26 双端保持核心功能兼容。
正式发布仍需另行安排。本轮先完成自动检查和内部 TestFlight 上传，用户安装后自行进行真机检查。

当前准备候选为 **1.7.0（154）/ `v1.7.0-beta.11`**。此前 149 被执行测试拦下；150 的执行卡顿已修复，但本地化键名检查失败，均未上传 Apple。定向 150 执行 + 83 安全 + 12 工具权限测试通过，双端原生组件最新通过证据为 `4550b6d`。下文保留历史失败与修复过程，不能把早期“待确认”当作最新结论。

## 当前已写入的链路

- 手记有独立入口、数据库、不可变资源、页面和导图模型、修订检查、持久化撤销重做、收藏与回收站恢复。最近列表单独记录打开顺序，重启保留且不改变文档修订或撤销记录；旧资料以原更新时间作为初始回退。
- PDF/图片与 Office 导入；PDF 原文提取用于搜索，原文与 AI 生成的批注保持区分。
- PencilKit 默认仅 Pencil 书写；手指书写单独开启。27 使用原生选中笔迹 ID，26 提供 AI 区域选择兼容入口。
- 选区合成页面背景与笔迹，并把来源、页码、区域坐标及真实图片送到现有完整聊天输入框，发送前仍可修改和选择模型。
- 手记独立助手复用现有任务与权限服务；首页草稿、普通对话和画布助手可显式选择手记，默认只读。首页发送时沿用草稿授权身份并在事务中创建任务，不为选资料创建空白任务。选择器显示并撤销授权，编辑提交时再次检查授权；撤销不删除已发送的对话副本。
- PDF 导出按页处理、保留批注、重新读取输出；思维导图支持 Markdown 大纲导出。`.floenote` 可编辑归档保留资源与来源身份，校验后导入独立副本。
- Mind Elixir 5.15.1 的本地桥接、节点/关联线编辑与事务保存；布局、样式和概要持久化，以及 Agent 完整结构编辑已写入；完整交互仍需验收。
- 导图主题图片使用手记资源存储，支持插入、替换、移除；缩略图有总像素预算，最多 64 个不同图片资源。保存回传后保持主题选中，网页链接按用户点击在编辑器之外打开。
- 导图主题支持图片、文档、音视频及其他文件附件，带说明、替换、预览与分享。文档关联、小窗、独立撤销与整组归档已写入；详见 [图文思维导图设计](FLOE_1_7_MIND_MAPS.md)。小窗和附件面板通过本地原生定向编译，完整交互待验收。
- Office 编辑后将经过保存的副本晋升为新的手记资源，原资源保持可恢复。
- 原生 Office 新增 PDF/Office/RTF/TXT 转换、演示放映及矢量画笔接口；应用依照宿主实际接口显示操作。
- WhisperKit 固定提交，Whisper Small 多语言的 27 个模型/分词器资源逐文件固定来源与摘要，约 491 MB 按需下载。
- 语音输入接入 Whisper 优先、失败回退 Apple；录音不持久化，缓冲有界，结束时等待识别收尾。
- 新增单独的 Whisper 实际推理资格工程：固定模型下载/校验、真实 Core ML 加载、无 Apple 回退的中英合成样本识别及时间戳检查；本地测试工程编译通过；云端实际推理结果见下方证据。合成语音不替代真人中英混说或真机成本验收。
- 视频自动字幕与 `audio.transcribe` 已接入同一文件转录服务，25 秒音频缓冲、按原素材时间戳定位，输出 SRT/VTT/JSON；本地定向编译通过，实际识别质量及双端运行测试仍待完成。

## 历次测试证据

- `34730814101` / `298576e` 的 SDK 26 iPad 通过 9 项组件与 1 项 UI 测试，截图确认透明书写层修复后 PDF 正文可见。iPhone 9 项组件测试通过，但横屏应用级截图截掉了上部正文，OCR 断言失败；`notes/render-regressions-298/` 保留复现。改用获授权 UI runner 的全屏 `XCUIScreen`，继续保留 OCR 与真实窗口宽高断言；窄屏和调整大小时，导图自动适应可用区域。新运行结果待确认。

- `34730524475` 在固定提交 `e57a905` 上云端通过全部 53 项平台测试，含 16 项媒体相关测试、四路并发和实际写入中的取消，约 152 秒含构建。此前 provider/cooperative pool 阻塞在该轮未再发生，记录 `platform-cloud-e57-*`；整包构建和真机验收仍独立进行。
- `f5ec5bc` 的 SDK 26 双端每端 9 项组件与 1 项 UI 测试通过，但人工检查 iPad 截图发现 PDF 空白、横屏截图不正确；因此不计为显示验收通过。图片保存在 `notes/render-regressions-f5/`。书写层透明叠加已经修正，资格工程补充与主应用一致的横屏声明，并增加真实窗口宽高与截图 PDF 文字 OCR 断言，避免再次仅凭 DOM 存在认定显示正确。

- `34730102365` 在 `f5ec5bc` 仍未通过：云端 SDK 27 beta 6 的 provider 内部重新进入 cooperative executor，外层自定义 actor executor 不能消除阻塞。保留 `media-stall-f5ec/` 采样。现将实际 `copyNextSampleBuffer` 与样本写入整体放入每路独立 GCD worker，单样本缓冲、取消后等待退出；修正后本地 53 项平台测试通过（约 65 秒），记录 `platform-worker-tests.txt`。新的云端执行待确认。
- PencilKit 书写层改为透明叠加，由页面底图绘制纸张/PDF，避免不透明书写层遮盖课件；本地原生定向编译通过，屏幕级显示待确认。

- `34728799329` 在 `0a38e84` 的云端测试仍发生停顿。采样表明原生 async provider 内部依然同步调用 `copyNextSampleBuffer`，不能只靠改成 async 消除线程池阻塞。每条音视频传输改为 GCD 支持的独立串行执行器，保留并发处理与取消等待；当前本地完整 53 项平台测试通过，记录 `platform-executor-tests.txt`，旧失败在 `media-stall-0a38/`，新云端结果待确认。
- `34728860282` 的 SDK 26 测试报告普通单元测试无权执行屏幕级 UI 截图。屏幕采集已移到正式 UI 测试 target，组件测试继续验证 DOM、文件和独立撤销；新增竖屏、横屏和关闭小窗后的显示测试，限定单项超时并留存图片。本地原生 UI 测试工程编译通过，运行结果待确认。

- 完整应用在 `57d1915fbb733e4289b0e63bb9fc2eb4a025964d` 的 SDK 26.6 兼容构建通过（`34728041064` / `103645661295`）。该运行整体仍失败于先前媒体并发测试，不能计为整轮 CI 通过；SDK 27 和修复后的媒体测试在新运行中验收。
- 小窗控件和来源导航补充通过本地 arm64 原生定向编译；新增来源失效时保留当前阅读器、独立小窗会话不变的回归测试，运行结果待云端验证。

- Whisper 推理运行 `34728314865` 在 `d51cad7ff516657a1f109e83a6eefbdd805df127` 上通过 iPad/iPhone SDK 27 模拟器。约 9 秒中英合成样本被识别为“今天我們學習Opportunity Cost,也就是機會成本,Tomorrow we will review international trade.”。iPad 模拟器模型加载约 3.94 秒、推理约 5.68 秒；iPhone 模拟器加载约 8.42 秒、推理约 8.11 秒，不能当作真机速度。完整下载校验约 491 MB，直接调用 WhisperKit，无 Apple 回退；记录 `evidence/floe-1.7/whisper/*d51cad7-inference.json`。尚未测真人语音、设备峰值内存或麦克风完整交互。

- 最近打开记录增加重启、只读打开不改内容/撤销、回收站排除测试，当前 14 项 Notes 主机测试通过；记录 `evidence/floe-1.7/notes/host-tests-recents.txt`。

- `34727968231` 在 `0f4b9248e178d694d2c62aec74e995c8aac938df` 上完成 SDK 27/26 iPad/iPhone 四种组合各 8 项、零失败零跳过，包含文档导图小窗与独立撤销。视图合成截图未捕获 PDF/WebKit 内容层，不用作功能介绍图片，后续改采屏幕级截图；双端摘要保存为 `*0f4b924-summary.json`。
- `34728041064` 的平台测试停顿取得了媒体栈证据：多个并发转码在 `copyNextSampleBuffer` 阻塞。26+ 改用 Apple 原生异步 provider/receiver，分别调度音视频并在失败/取消时等候读取任务结束。10 项真实媒体测试本地通过，含四路并发与写入开始后的取消，日志 `evidence/floe-1.7/media-async-provider-tests.txt`；旧停顿样本在 `evidence/floe-1.7/media-stall-57d/`。完整云端重测仍需完成。

- 图片运行 `34727282394` 的 SDK 26 iPad 失败于测试将点尺寸当作 Retina 像素尺寸；实际截图确认图片已显示。断言改为读取源 PNG 像素尺寸，在新提交上重新验证，不把该失败计为通过。完整 App 运行 `34727296803` 进一步发现 Office 资料附件调用缺少 `displayName`，已补入原文件名，等待完整应用重编译。

- 图文附件与关联归档增加 3 项测试，当前 13 项 Notes 主机测试通过：包含附件字节恢复、内部来源重映射、独立撤销、错误导入回滚和单独的 Agent 关联授权。记录 `evidence/floe-1.7/notes/host-tests-linked-maps.txt`。

- `Qualification/Notes` 的 6 项主机测试通过：保存恢复、撤销重做、资源去重、事务失败/过期修改、幂等回执、权限/回收站、导图循环和中文检索。
  新增归档往返、篡改拒绝与导图概要删除/撤销测试通过，共 8 项；记录 `evidence/floe-1.7/notes/host-tests-map-history.txt`。
  早期原始记录：`evidence/floe-1.7/notes/host-tests-initial.txt`。该记录是早期组件快照，后续修改需重新运行。
- 新增授权撤销、重启恢复、其他会话保留及提交时权限检查后，9 项主机测试通过，记录 `evidence/floe-1.7/notes/host-tests-revoke.txt`。Node 宿主与导图桥接共 5 项契约测试通过。
- 草稿任务身份有 2 项独立测试通过：首次创建沿用身份、重复身份不得覆盖旧任务；失败预检不创建空任务且身份可重试。记录 `evidence/floe-1.7/notes/draft-launch-tests.txt`。
- 原生运行 `34725371828` 的 SDK 26 iPad 实测暴露 Mind Elixir 菜单 locale 参数错误，导致导图初始化失败；已按固定版本的对象接口修正并增加桥接测试。此失败记录不计为通过，双端重测需以修复后的提交为准。
- 修复后的原生运行 `34726144232` 在提交 `4b19403bf58f5f718286ad95f0251dd9d3d73891` 上通过两个 SDK 的 iPad/iPhone 四种组合，每种 7 项、无跳过。新增真实 WebKit 渲染、长回答分页、立体声音轨重采样及延迟对齐、SRT/VTT/JSON 导出测试；Whisper 权重推理仍未包含。27 双端摘要与导图截图保存在 `evidence/floe-1.7/notes/*4b19403*` 和 `ipad27-mindmap-literal-text.png`。
- 原生定向工程在云端运行 `34723388987` 通过 Xcode 27 与 Xcode 26.6 的 iPad/iPhone 四种组合，每种 3 项测试。固定提交 `6897ce045981b28450ffba55eb3ac399bc2e16c9`，验证 PDF 笔迹与可搜索文字、损坏笔迹拒绝及 Whisper 资源清单；不包含 Whisper 推理或完整 App UI。后续归档、页面图片和回答保存修改仍需新一轮验收。
- 平台诊断运行 `34724626620` 在提交 `086b5a38fbdfe9954d8992bdf02b5d55a9f72dd8` 上完成 49 项测试（1 + 1 + 18 + 14 + 15），耗时含构建约 151 秒。日志和摘要保存在 `evidence/floe-1.7/platform-tests-20260913*`；先前 SwiftPM 测试停顿未在此轮重现，保留超时与进程采样，不能据此宣称已找到停顿根因。
- Office 原生宿主云端构建 `34722048321` 成功；固定提交 `5911fd3155bdfbf78c461bcd647116bd9b79bbee`。
  产物校验通过 4785 个文件及 178 个目录。仅表示宿主编译、链接、Swift 导入及资产完整性，不表示真机操作通过。

## 仍须完成或验收

1. 手记完整 UI、页面图文操作、回答保存与分页、Office 恢复入口均已继续接线，仍需完整交互与保存重开验收；历史来源定位、完整文件生命周期及资料索引仍待完善。
2. 原生 PDF/套索/缩放测试；实际 Word 画笔、各种格式导出及 PPT 放映。Excel 暂缓新增增强功能。
3. Whisper 实际安装/加载、中英混合样本、错误回退、长录音和内存；媒体转录已接线，剩余录音入口与实际长文件链路仍需核验。
4. 原计划中的环境生命周期与迁移、共享任务队列、软件包制作与生产签名源、媒体模型真实运行器与目录交付。
5. 15 类媒体能力的真实输出与设备成本，33 个模型和 15 个软件包的最终逐项结论。
6. 完整应用构建、旧功能回归、当前与发布 SDK、iPad/iPhone 实体设备验收，以及固定提交的 TestFlight 分发。

## 截图与文档

组件测试使用 `.keepAlways` 留存可视证据。完整应用截图优先 iPad，补充 iPhone，记录设备、系统、场景与对应代码版本。
组件验证页或旧版界面不能作为新版本完整产品截图。去除敏感信息后，将符合最终界面的图片用于中英文 README、使用说明和版本说明。

## 已保留的图片证据

![iPad 27 重新读取导出 PDF 中的中文、英文和真实笔迹](evidence/floe-1.7/notes/ipad27-export-text-and-ink.png)

这张图来自上述固定提交的导出文件，不是完整手记界面截图。来源测试：`NativeNotesTests/testPDFExportRetainsPagesInkAndText()`；设备与结果见同目录 `ipad27-component-summary.json`。

## 2026-09-13 动态导图与回归修复

- `a714962`：内容尺寸驱动的导图连线刷新、批量事件合并、独立文档首次适配，连续编辑保留缩放；修复 Agent 修改布局方向未传入引擎的问题。桥接测试及原生组件 build-for-testing 通过，新增真实 WebKit 长标题/附图/分支无重叠断言。云端运行 `34741996361` 尚待结果。
- `b46e089`：修复 Word/Workbook 执行器存在但发现目录缺失的问题；脚本型 Skill 显式声明工具依赖，不再提取 Python 属性和已退役工具名；工具名解析排除通配名称。
- 上一轮 `34731780689`：两套 SDK 的 iPad 测试通过，iPhone 页首 OCR 失败；检查完整横屏截图确认是阅读位置偏移造成页首裁切，不能将其当截图工具错误或改低断言放过。
- 上一轮 CI `34730532393`：SDK 26 构建及 Linux 成功；SDK 27 的 135 项 App 回归中出现 6 个工具目录/Skill 依赖问题，尚未由新提交复测关闭。53 项平台测试已有独立通过证据。
- 此记录不表示整个 1.7 已验收，也未上传 TestFlight；包/模型交付、完整功能回归和双端真机证据仍须逐项完成。
- 后续补丁：PencilKit 视口变化时保留页面坐标位置，合并重复首次缩放入口；本地原生组件 build-for-testing 通过，页首 OCR 仍等待云端实际复测。使用真实 BundledDomainSkills 与生成的官方定义编译执行了依赖检查，通过。

## 手记生命周期与描述文件收尾

新增回收站永久删除（确认后提交、检查修订），移除该资料历史、索引、回执与未来助手授权；独立关联导图不级联删除。资源回收单独登记，并保留其他资料、撤销/重做、回执和活动读取 URL；多个存储句柄共享读取保护。文件删除失败留下队列，下一次打开重试。15 项 Notes 主机测试通过，证据 `notes/host-tests-permanent-delete.txt`；原生组件 build-for-testing 通过，新增根界面操作仍需整包和真机验证。

新版描述覆盖双语 README/指南、架构、迁移恢复、构建验收、变更草稿、TestFlight 准备记录、截图用途说明以及 `RELEASE_NOTES_1.7.0.md`。历史版本记录保留。上传状态仍为空，不把开发分支描述成已经可安装的新版本。

最新 iPad 和 iPhone 的 devicectl DDI 服务均可用，之前的锁屏阻碍本次不再出现；已启动签名原生组件的设备构建，尚未证明真机安装、运行或交互通过。

用户指定真机检查自行执行。已停止本地真机组件构建准备，没有安装或启动真机应用。本轮继续负责代码、自动化测试、云端构建、描述文件和 TestFlight 上传；真机验证交接不再阻塞上述工作，但其结果保持待用户确认。

## TestFlight 149 流程与执行测试补充

`9edbeead0038fbe7d64e36c589e7d77a26c8224b` 固定为 `v1.7.0-beta.6`、build 149，版本/扩展一致性预检通过。发布流程为 `34744967142`，尚未完成上传。旧 CI `34742128610` 的 135 项 App 回归实际通过，SDK 26 构建和 Linux 目标通过；跨平台工具测试发现 Canvas 的预期工具集漏掉四个已接入的 Notes 工具，执行模块还有停顿，旧运行已停止并保留日志。

后续修改更新工具集断言；WASM 阻塞解释器/输出管道改为独立 GCD worker，Swift 任务取消传入执行预算，仍等待 worker 退出才返回结果。新增无外部 token 的任务取消与 12 路并发执行测试；这些修复需在新提交重新通过后才能形成后续上传候选。CI 也沿用发布脚本的输出留存、停顿采样及超时停止，不跳过原有测试。

149 的发布运行已失败，未上传。`execution-stall-149/` 保留真实云端采样：三个 cooperative worker 停在 WASM 输出管道的 `dispatch_group_wait`，同时网络 deadline 测试超时失效。150 候选将 WASM 和阻塞 DNS 工作移出该线程池，保留原有验收门槛，新增并发 deadline 断言。当前仍在定向测试与新候选准备阶段，不能声称可安装。

150 修复的定向测试已通过：150 项执行测试、83 项安全测试；12 路 WASM 命令、无外部 token 的任务取消、8 路阻塞 lookup 的超时均通过。保留 `execution-stall-149/fixed-local-tests.txt`，仍需新标签的云端完整流程。

## 151 本地化修复

150 云端完整 Swift 运行正常结束，只有 `FloeCoreTests` 的命名空间检查失败，其余测试目标未报告失败。环境/主题的 21 个原文键改为命名空间键，所有引用同步，中英文译文原样保留；动态 hold/unhold 文案显式使用本地化键。原测试规则不变，下一候选重新走完整发布流程。

151 的固定源码 `8d370cf` 已在云端 `34746784780` 通过完整 Swift 测试、两组 JavaScript 独立测试、53 项平台和 15 项手记测试，共 1244 次 Swift 测试执行；环境/包/媒体/Node 专项步骤通过。证据 `release-151/swift-qualification.json`。现在进入完整 iOS Release 构建，尚未完成签名上传。

## 152 分发扫描修复

151 通过 SDK 27 Release 整包和全部 135 项 App 回归（0 失败、0 跳过），随后在扫描时把 Whisper 的公开 tokenizer commit 摘要报成 generic-api-key。已核实公开来源，仅登记具体历史指纹；当前未被运行时读取的元数据字段改名为 `textAssetsRevision`，所有模型文件 URL/hash 不变。源码扫描提前到重型验证之前，App 回归产物在后续失败时也留存。151 未上传，下一候选仍需完整发布流程。

152 固定提交 `8e43aed` 的云端 `34748849626` 已通过源码历史扫描及全部 1244 次 Swift 测试执行，正在整包构建。后续远程 Agent 分发的本地预检通过：5 项任务取消、4 项 PTY 生命周期及打包摘要校验。当前构建仍未上传 Apple。

## 152 结果与 153 发布脚本修复

152 / `8e43aed` / 云端运行 `34748849626` 的 1244 次 Swift 测试执行和 135 项 App 回归全部通过，SDK 27 模拟器 Release 构建通过；尚未上传。随后许可证清单读取 `Package.resolved` 时缺少顶层 `pins` 而失败。153 支持 SwiftPM v1/v2/v3 锁格式，拒绝未知/空锁、重复及可变依赖，并比较提交中的依赖来源与版本/修订，防止格式兼容掩盖依赖漂移。5 项锁格式回归和 197 项本地清单生成通过。真机检查仍由用户在 TestFlight 安装后执行。

153 / `1ae0364` / `34751318198` 在项目生成检查提前失败：`project.yml` 已更新构建号，生成的 `.pbxproj` 仍为 152。154 同步生成文件；发布前重复生成并核对完全一致。153 没有上传，不计作 App 测试结果。
