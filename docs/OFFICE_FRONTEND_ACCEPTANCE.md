# Office 前端逐项实施与验收

本文件是 [本轮升级总计划](WORKFLOW_UPGRADE_IMPLEMENTATION.md) 的 O01–O05、F04 明细，不缩减其他升级目标。2026-09-09 用户再次确认：**接入完整编辑器界面不等于完成编辑功能；不能只验证文字和单元格便宣称完整 Office。**

当前结论：Office 前端仍在实施，未完成。下表中的“局部验证”只对应明确记录的样本操作。现有上游菜单、UNO 命令或导出 API 的存在，均不能代替用户实际操作成功。

最新连续保存回归（原生宿主 34344605828，独立 Mac 应用 20）：附件撤销/重做、两次保存重开与三次原字节导出通过，固定页面锚定方式不再在重开后翻转。字体度量候选失败：未编辑列 A 为 22 → 22.82 → 21.8，附件宽/高仍有微小变化。详见 [运行记录](evidence/workflow-upgrade-20260909/office-xlsx-font-anchor-runtime.json) 和 [几何比较](evidence/workflow-upgrade-20260909/office-xlsx-font-anchor-repeated-layout.json)。这仍是保真失败，没有完成 Office 或发布的结论。

最新关键结果：Word 完整 Floe 原文件附件闭环已通过；Excel 新宿主 34340935668 在独立应用 19 通过撤销/重做、两次保存重开、三次附件导出字节一致，每次保存均 30 项结构检查通过，C10 新文字也保存正确。但未修改附件的横向位置/宽度和工作表默认列宽跨保存发生漂移，保真仍失败。PPT 图表工作簿、其他完整操作与真机继续待验。

## 统一操作与证据规则

- 右侧检查器只读，进入全屏后才允许编辑；新增按钮和快捷键也必须遵守这个边界。
- 采用正常文档页面、表格网格和幻灯片画布，保留选区、光标和对象操作；禁止退化为逐字段表单。
- 每项分别记录：入口可达 → 用户完成操作 → 撤销/重做 → 原格式保存 → 关闭重开 → 内容/位置/样式对照。缺少任一相关环节，不能标为完整通过。
- 数据检查和视觉检查分开：值正确不等于格式正确，坐标存在不等于画面正确，附件关系存在不等于附件字节完整。
- 独立 Mac Designed for iPad 测试、完整 Floe 文件会话、物理 iPhone/iPad、Microsoft Office 重开分别记录。不能相互替代。
- 每个故障保留输入样本、步骤、截图、保存产物摘要、实际失败项；未修改对象也要验证是否被意外改动。

## 附件必须实现的完整行为

附件指真正随 DOCX/XLSX/PPTX 保存的文件或嵌入对象。普通超链接、文件名文本、截图、旁边保存的原文件，均不能代替附件嵌入。将 PDF 页面转成图片插入，与把 PDF 原文件作为附件插入，是两个不同操作。

| ID | 用户操作与预期 | 当前状态 / 待实现或验证 |
|---|---|---|
| A01 | 全屏“插入附件”可从系统文件和 Floe 工作区选文件，取消后不改变文档 | 局部：开发组件对照完整应用的 Word 工作区选择已实测，停留 35 秒后返回正常；系统文件与取消路径、真机仍待验收 |
| A02 | 文件在异步导入前复制到该编辑会话，离开外部授权范围后仍可保存 | 实施中：文件协调、授权读取、会话 source 子目录复制与操作互斥已写入；实际失败/恢复未验收 |
| A03 | Word 可插入作为图标/文件名展示的附件，放在当前光标位置；支持对象锚点与环绕 | 局部：独立编辑器及开发组件对照完整应用均完成插入、保存重开，图标和当前段落保留；锚点/环绕调整和真机未验收 |
| A04 | Excel 附件作为工作表对象放在选定区域；支持位置、尺寸及随单元格移动行为 | 部分通过：编号修复后第二张表 B5 插入、保存重开、两次导出字节一致，29 项结构检查通过；新组件撤销重做保存、精确尺寸/位置、随单元格移动及完整 Floe 仍待验收 |
| A05 | PPT 附件放入当前幻灯片；支持位置、尺寸、层级和组合边界 | 局部：实际插入第二张幻灯片、保存关闭、只读重开及前后导出字节一致，16 项结构检查通过；新插入图标未即时显示，重开才显示，需修复并验证位置尺寸及层级组合 |
| A06 | 文件名、类型、图标正确，中文、空格及特殊字符不损坏 | 局部：34312836612 保存样本的 Unicode 名称、ASCII 兼容名称、Package 类型和图标关系通过；其他类型/图标及 Microsoft Office 未验收 |
| A07 | 选择、移动、调整尺寸、删除、撤销和重做保持附件与显示对象一致 | 局部：34313654241 两轮单次撤销/重做实际通过，保存后 ProgID、图标及原字节保留；移动尺寸、删除、完整 Floe 等仍未验收 |
| A08 | 保存并关闭后重新打开，附件仍存在，位置不变；可查看或导出附件 | 局部：独立编辑器完成插入/导出/撤销重做/重开再导出；开发组件对照完整应用实际写回原文件，新文档会话再次打开可见且原附件字节一致。完整重建、应用内导出、精确位置和真机仍待验收 |
| A09 | 导出的原附件与输入文件哈希一致；嵌入 Office 对象若被用户编辑，按其原格式另行验证内容 | 局部：原生导出及独立 DOCX 提取的 65,539 字节附件均与输入 SHA-256 一致；完整 Floe 导出、多格式/大小及可编辑 Office 对象仍未验收 |
| A10 | 替换附件、同名附件、连续插入、取消选择、读取失败、大文件失败均不留下损坏对象 | 未实现 / 未验收 |
| A11 | 存在原文件并发修改或保存失败时，保留含附件的恢复副本，允许另存 | 现有恢复基础需与真实附件场景贯通 |
| A12 | 未编辑的现有附件/嵌入对象随其他编辑完整保留，Microsoft Office 可打开 | 未验收；是独立于“新增附件”的要求 |

通用文件封装、原生 Office 嵌入对象和音视频使用各自正确的原格式关系及内容类型，不盲目把任意文件传给图片插入命令。若锁定的移动引擎无法完成某类对象，必须补齐相应处理或明确记录实现阻塞；不能静默跳过、放置占位按钮，或将该类功能标成完成。

## Word 操作明细

| ID | 操作范围与保存要求 | 当前证据 |
|---|---|---|
| W01 | 输入、选区、中文输入法、剪切复制粘贴、查找替换、撤销重做 | 局部：粘贴正文并重开成功；键盘自动化丢字符、撤销残留尚未解决或真机复核 |
| W02 | 字体、字号、字重、斜体、下划线、文字色、高亮、字符样式 | 局部：前台直接打开字体菜单后，指定 survive 改为 24pt 加粗、单次撤销/重做加粗、保存重开通过；其他文字保持原 18pt 蓝色。后台 AX 菜单关闭故障和其他属性仍待查/验收 |
| W03 | 段落对齐、缩进、行距、段前后间距、项目符号、编号与段落样式 | 未逐项验收 |
| W04 | 插入表格、增删行列、合并拆分、宽高、边框底纹、单元格对齐 | 局部：已有简单表格内容保留；用户操作未验收 |
| W05 | 插入/替换图片、裁剪、比例、尺寸、旋转、锚点、环绕与层级 | 未逐项贯通验收 |
| W06 | 插入图表、选择类型、编辑系列/分类/数据、标题与图例、位置尺寸 | 未验收；其他样本已有首数据点显示异常，不能以缓存正确判通过 |
| W07 | 附件与嵌入对象 A01–A12 | Word 原生附件插入/保存链路已有局部实测；完整操作、恢复和兼容性仍未完成 |
| W08 | 纸张大小、方向、页边距、分页/分节、页面背景与边框 | 局部：已有纸张/页边距保留；修改未验收 |
| W09 | 页眉页脚、页码、首页不同、奇偶页、脚注尾注、目录/域更新 | 局部：已有简单页眉保留；其他未验收 |
| W10 | 超链接、书签、评论、修订接受/拒绝、文本框与形状 | 未逐项验收 |

## Excel 操作明细

| ID | 操作范围与保存要求 | 当前证据 |
|---|---|---|
| X01 | 网格选择、单元格/区域编辑、公式栏、复制粘贴、填充、撤销重做 | 局部：公式栏粘贴 B2=36 并保存重开；其他待验收 |
| X02 | 数字、文本、日期、百分比、货币、自定义格式与区域语言 | General 异常语言路径已修复；日期/货币/百分比及多语言混合格式待验收 |
| X03 | 字体大小颜色、边框底纹、对齐换行、合并取消合并、条件格式 | 局部：已有简单表头样式保留；用户修改待验收 |
| X04 | 增删/移动/隐藏行列、行高列宽、冻结窗格 | 局部修复：34348631751 两轮保存 A 列宽均为 22，全部未改列宽稳定；实际行列操作及多字体/真机仍待验收 |
| X05 | 新增/重命名/移动/复制/删除工作表、跨表引用 | 已有两张表顺序/简单内容保留；实际管理操作待验收 |
| X06 | 公式输入、跨表计算、重算、错误值、函数向导 | 局部：B2*2 重算并缓存 72；其他待验收 |
| X07 | 排序、筛选、表格区域、数据验证与条件规则编辑 | 未逐项验收 |
| X08 | 插入图表、更改类型、编辑数据源/系列/坐标轴、标题图例、移动尺寸 | 首点显示异常未解决；已有图表锚点和缓存保留不等于图表操作成功 |
| X09 | 插入/替换图片、形状、附件与嵌入对象 | 未逐项贯通验收；新增附件通道正在编译及运行验证 |
| X10 | 打印区域、分页、纸张方向、缩放、页眉页脚与导出 | 未逐项验收 |

## PowerPoint 操作明细

| ID | 操作范围与保存要求 | 当前证据 |
|---|---|---|
| P01 | 创建、复制、删除、排序幻灯片与节，缩略图和当前页面同步 | 仅观察到两页缩略图；实际操作/保存未验收 |
| P02 | 选择布局、主题、母版、页面比例与背景 | 未逐项验收 |
| P03 | 文本框、中文输入、文字/段落格式、项目符号、溢出和自动适配 | 未逐项验收 |
| P04 | 插入/替换图片、裁剪、比例和透明度 | 未逐项贯通验收 |
| P05 | 形状、连接线、位置、尺寸、旋转、对齐分布 | 局部：前台位置/尺寸对话框及增减控件修改 X=3 cm、宽=8 cm、高=6 cm，保存重开数值精确；直接键盘输入、后台 AX 失败、其他形状操作仍待解决/验收 |
| P06 | 对象层级、组合/取消组合、多选、锁定或合理选择行为 | 未逐项验收 |
| P07 | 插入和编辑表格、行列尺寸、边框底纹、合并拆分 | 未逐项验收 |
| P08 | 插入和编辑图表、系列和数据源、图例轴标签、对象位置 | 首柱预览缺失；原图表缓存保留但保存后内嵌 Excel 工作簿丢失，失败；新增/编辑仍待验收 |
| P09 | 附件、Office 嵌入对象、音频视频与播放/导出 | 新增通用附件通道正在编译及运行验证；音视频现有通道仍需实际验证 |
| P10 | 演讲者备注、放映、已有动画/切换保留；支持的动画编辑明确验证 | 未逐项验收，不能把未支持的编辑静默删除 |

## 跨功能保存与错误处理

| ID | 必须通过的闭环 | 当前状态 |
|---|---|---|
| C01 | 原生工具栏“保存”与 Floe 保存按钮都最终写回正确的原文件版本 | 1.6.1 中未贯通，差异已写入测试说明。后续开发已用 UI_Save 事件接入相同的原文件保存/CAS/校验链，保存后保持编辑；重复点击合并，自动保存不触发原文件提交，原文件确认前不显示保存成功。实际桥接脚本检查、Swift 6 iOS 类型检查通过，完整 App 编译与真实工具栏保存重开仍待验收 |
| C01a | 原生菜单的另存与格式导出也经过对应格式的内容检查，再交付文件；保留现有导出格式功能 | 未贯通：上游 downloadas 直接使用原生 saveAs 和文件选择器，没有经过 Floe 的 prepareExport。当前保存保护仅覆盖 Floe 的保存/另存入口；已明确写入测试版限制，仍需统一导出回执与校验，不能靠禁用所有导出替代 |
| C02 | 快速连续保存、自动保存、切换预览、关闭、后台恢复不会乱序或丢操作 | 部分接口回归通过，完整前端与真机待验收 |
| C03 | 异常退出、内容进程终止、磁盘不足、附件导入失败可恢复 | 部分副本保留基础具备；每个真实失败场景待验收 |
| C04 | 原文件被模型或其他编辑器修改时有冲突处理、另存、恢复版本 | 文件会话基础已实现，带完整 Office 对象的实测待验收 |
| C05 | iPhone/iPad 触摸、外接键盘、输入法和辅助功能完成全部相关操作 | 未通过完整验收；不得用 Mac 自动化替代 |
| C06 | 实際 model 工具说明、SKILL.md、插件介绍只宣称已实现并验证的能力 | 所有功能结束后再次进行 H06 最终实际注入审查；当前不得宣称完整 Office 已完成 |

## 已核实的通用附件断点与实现顺序

1. `browser/src/control/Control.NotebookbarWriter.js` 等菜单具备图表/图片等上游入口，不构成通用附件实现。
2. `browser/src/map/handler/Map.FileInserter.js` 的移动文件导入通过 Base64 `insertfile` 消息处理现有类型；需要独立附件传输契约、错误回执与会话文件生命周期，避免将大附件作为模型文本重新生成。
3. `kit/ChildSession.cpp` 的 `insertFile` 类型列表和 `postInsertCommand` 目前覆盖图片、背景、比较和音视频；移动分支断言仅 graphic/multimedia。没有通用 attachment/object 类型。必须补齐处理，不能复用图片分支而错误报告成功。
4. `ios/Mobile/DocumentViewController.mm` 的 `UIDocumentPickerDelegate` 目前处理导出临时文件清理，没有通用附件导入回调。需要区分导入/导出任务、取消和错误、授权读取与会话副本。
5. `engine/sw/source/uibase/shells/textsh.cxx` 中 `.uno:InsertObject` 的处理读取对象类 ID，而不读取文件 URL；不能只传 `FileName` 便声称插入附件。`SwWrtShell::InsertObject` 已有嵌入对象/图标逻辑，应复用正确的对象创建路径并验证移动构建能力。
6. 先打通 Word 一个真实嵌入附件闭环，再落实 Excel/PPT 对象锚点与各自保存格式；继续验证新增附件、修改附件及“未修改附件保留”三类场景。此顺序不免除上表任何其他功能。

优先修复实际缺失入口和传输/嵌入/保存链路，再按上述矩阵完整实测。最终发布说明、模型工具和 Skill 介绍必须与逐项结果一致；不能用功能列表或上游宣称替代验收。

### 附件实现资格记录（2026-09-09）

- `d60a7a5` 首次附件宿主资格构建 `34311448140` 失败：UIKit 的旧 MacTypes 定义与引擎的 Point/Size/TimeValue 冲突，尚未执行附件运行测试。随后 `da2a453` 将附件引擎代码分离到纯 C++ 翻译单元，避免混入 UIKit 类型；重新资格构建仍需实际结果。
- 选择文件先复制到会话的独立 `source/` 目录，与生成的 OLE 包和预览图隔离，避免用户文件名与内部文件名相同而覆盖原附件。
- Word 使用引擎现有复合存储、导入解析器和文本嵌入对象；不在编辑器打开时旁路修改 DOCX 压缩包。公开原生 API 仍拒绝未实现的 Excel/PPT 附件类型，不能据此宣称三类功能完成。
- 独立资格应用新增 Unicode 文件名、65,539 字节二进制附件插入操作。`verify_office_attachment.py` 使用独立 `olefile` / `oletools` 读取保存产物，检查真实 OLE 关系、原字节哈希、文件名、图标和所在段落；脚本存在不等于已获得通过结果。
- `34311790525` 实际运行已完成插入、关闭重开和附件原字节提取；重做使内部存储改名而丢失 ProgID，且需要两次撤销。详见 [首次运行回执](evidence/workflow-upgrade-20260909/office-word-attachment-initial-runtime.json) 与保存结构对照。
- `34312836612` 编码修复保存样本 **18/18 结构检查通过**，包括 UTF-16 原名、旧式 ASCII 兼容名、真实嵌入类型、图标和原字节哈希，见 [结构回执](evidence/workflow-upgrade-20260909/office-word-attachment-encoding-fixed-structure.json)。该版本的 UNO 撤销分组缺少当前视图 ID，触发协作撤销归属冲突；这份结构通过不能代表撤销通过。
- `34313654241` 已通过云端编译/链接/Swift 导入；改用文档原生 `SfxUndoManager`，把分组和元数据重做动作归属到当前编辑视图。独立 Mac 应用 11 实际通过 **两轮单次撤销/重做 → 保存 → 关闭 → 只读重开**，最终 **18/18 结构检查通过**，包括重做后的 Package 类型及原字节哈希。见 [实际运行回执](evidence/workflow-upgrade-20260909/office-word-attachment-owned-undo-runtime.json)、[保存结构](evidence/workflow-upgrade-20260909/office-word-attachment-owned-undo-structure.json) 和配套截图。该结果只覆盖 Word 原生插入闭环；系统/工作区选择、附件打开/导出/替换、其他对象操作、完整 Floe 写回、Excel/PPT 与真机仍未完成。

- `34315555634` 原生附件枚举和导出宿主云端资格构建通过；从当前 Word 嵌入对象读取 Package 内容，不依赖插入时的旁路源文件。前端工作区选择、附件列表、查看及存储到文件入口已写入，原字节导出与完整 Floe UI 仍需实测。
- 完整应用构建 `34314992225` 失败：工程硬编码旧宿主目录 `34296658499`，导致原生模块未被导入且最终链接失败。已改为由已验证宿主生成 `native-host.xcconfig`，Debug/Release 共用该路径，并新增版本切换回归；13 项 bootstrap 测试通过，完整应用需重新构建。
- Word 字体菜单实际触发 WebContent 关闭；随后重开对照出现连接中断。原因未确认，见 [故障回执](evidence/workflow-upgrade-20260909/office-word-font-menu-failure.json)。不能以已有样式保存成功覆盖此操作失败。

- `34315555634` 真实附件列表在插入前、插入后和重开后都返回 0，独立读取 DOCX 则 18/18 结构检查通过。`34316463516` 改从复合文件本身识别 Package，已通过云端资格构建，导出运行结果仍待验证。
- Word 格式前台点击对照完成指定词语 24pt、加粗、加粗单次撤销/重做、保存重开；原 18pt 蓝色上下文保留。见 [回执](evidence/workflow-upgrade-20260909/office-word-font-24-bold-roundtrip.json)。粘贴字号时遇到输入焦点问题，最终用实际输入框提交正确值；不据此宣称所有输入方式或字体功能完成。
- `34316188417` 被工程一致性检查拦截：已生成的 `project.pbxproj` 未同步提交。随后同步生成工程与两个附件页面，并修正意外关闭后异步操作可能把界面重新标为可编辑的状态。完整应用仍需重跑。

- `34316463516` 实际附件列表由错误的空列表变为读取失败。`34317744039` 已通过编译/链接/Swift 导入，改用存储已提交快照读取，并保留固定内部错误诊断；新的独立验证应用已本地构建，尚未获得该版本导出结果。
- 完整应用 `34317224747` 编译发现附件导出 catch 内错误变量遮蔽，已由 `b83de12` 修正；完整应用仍需重跑验证。

- `34317744039` 独立 Mac 编辑器实际枚举并导出 1 个中文名附件，65,539 字节，SHA-256 与原文件一致，见 [运行证据](evidence/workflow-upgrade-20260909/office-word-attachment-snapshot-export-runtime.json)。随后点击撤销出现服务连接中断，保存持续等待；不能把导出局部成功记作完整保存/撤销通过。
- 对保存持续等待补齐两个失败出口：发送前检查编辑器连接；90 秒内未收到匹配回执则明确失败并保留副本。9 项实际编译的回执顺序/超时检查通过，见 [回执测试](evidence/workflow-upgrade-20260909/office-save-timeout-contract.json)。新宿主运行仍待验证。

- 新建未插入/读取附件的 Word 对照会话也在点击“编辑文档”后出现连接中断，说明该故障并非只发生在附件导出之后；尚不能归因于附件读取。见 [对照证据](evidence/workflow-upgrade-20260909/office-word-edit-connection-control.json) 和配套截图。下一步继续区分后台/辅助功能点击与前台直接操作，并验证实际 iOS 会话。

- `34320630546` 宿主已校验并接入；前台坐标操作成功完成 Word 插入、导出、撤销/重做、保存关闭、只读重开及再次导出，两次 65,539 字节均与原文件一致，18/18 文档结构检查通过。见 [运行回执](evidence/workflow-upgrade-20260909/office-word-attachment-foreground-runtime.json)、[结构检查](evidence/workflow-upgrade-20260909/office-word-attachment-foreground-structure.json) 和 [重开截图](evidence/workflow-upgrade-20260909/office-word-attachment-foreground-reopen-export-mac.jpg)。该对照未复现后台 AX 断连，但不证明其修复；保存超时保护只通过编译/单元检查，故障场景运行验证仍待完成。

- P05 前台对照：宿主 34320630546 实际打开位置/尺寸对话框，通过数值增减设置 X=3 cm、Y=5.08 cm、宽=8 cm、高=6 cm，保存关闭和只读重开后四项 EMU 坐标精确一致。直接输入没有确认生效，不能视为已通过。新增 [运行回执](evidence/workflow-upgrade-20260909/office-ppt-position-size-runtime.json)、[严格结构检查](evidence/workflow-upgrade-20260909/office-ppt-position-size-structure.json) 和输入/重开截图。检查同时确认：保存后图表缓存的 Alpha=12、Beta=24 保留，但原内嵌 Excel 数据文件及对应关系从 1 变 0；预览仍缺首柱。未修改标题/图表/第二页对象宽高各有 360 EMU（0.01 mm）漂移，页面宽有 1 EMU 取整差异。以上全部保持失败，不勾选完整 PPT/保真通过。

- P07/P09 工作簿丢失已定位到锁定上游源码：`engine/oox/source/export/chartexport.cxx` 的 `ChartExport::exportExternalData` 在 2078 行只允许 DOCUMENT_DOCX，PPTX 直接返回。见 [源码与运行关联诊断](evidence/workflow-upgrade-20260909/office-ppt-chart-workbook-export-diagnosis.json)。修复需同时处理图表数据文件、关系和单元格引用，覆盖修改数据后的导出；只放开条件或只保留缓存/旧工作簿都不能满足可编辑图表要求。

- 34327287597 新宿主在独立 Mac 应用 16 完成 Excel/PPT 附件对照：Excel 两组保存均丢失附件，PPT 保存及重开导出原字节一致，但插入后页面未即时显示。见 [运行记录](evidence/workflow-upgrade-20260909/office-drawing-attachment-runtime.json)、[Excel 直接保存失败](evidence/workflow-upgrade-20260909/office-excel-attachment-direct-structure.json)、[PPT 结构检查](evidence/workflow-upgrade-20260909/office-ppt-attachment-structure.json)。保存接口均完成，不能用该成功结果代替附件保真。新增对象选中/变更通知及全屏初次自动进入编辑的修复仍需新宿主运行验证。

## Excel 导出修复进行中

最新补查（2026-09-09 14:41 UTC）：独立应用 24 使用宿主 34354533462，已打开随单元格缩放样本并显示附件。操作名称框及剪贴板时，WebContent 在 AX 属性读取错误后报告 Swift 数组越界并以 Crash 原因终止；容器记录 `unexpectedClose`，两个工作副本仍存在且哈希一致。未进入显式保存重开，不能将此样本计为位置/尺寸通过；触发根因及物理设备影响尚未隔离。见[运行记录](evidence/workflow-upgrade-20260909/office-modern-ole-interrupted-runtime.json)、[系统日志摘录](evidence/workflow-upgrade-20260909/office-modern-ole-webkit-failure.log)和[截图](evidence/workflow-upgrade-20260909/office-modern-ole-unexpected-close.png)。该独立 Mac 检查不代替正式 Floe 的 CI 或真机测试。

对应正式 App 处理链已复核：上游 `webViewWebContentProcessDidTerminate` 调用 `bye`，补丁保留引擎副本并在关闭完成后通知宿主；`OfficeFileSession.activate` 的 `onClosed` 将非预期关闭置为 `runtimeFailed` / `.failed`，提示“文档已关闭，编辑副本已保留。”，`canAct` 不再允许保存等正常操作。此处为源码审阅，未把独立程序的崩溃现场当成正式 App 的恢复实测。

原生 `XclObjOle` 缺少 XML 导出的修复已实际编译为 arm64 对象；只替换 `libscfiltlo.a` 中的导出对象，另外 167 个成员哈希不变。新导出读取当前对象存储，关联工作表、附件、图标和锚点；不是保存后修改 ZIP。见 [回执](evidence/workflow-upgrade-20260909/office-excel-filter-overlay-compile.json) 和 [补丁边界](../FloeAgent/ThirdParty/Collabora/patches/xlsx-embedded-objects.md)。实际保存重开、导出字节、多个对象、撤销重做与位置保真仍待验收，不能将本次编译结果作为 A04 通过证据。

完整应用 Word 证据：[415dad3 运行闭环](evidence/workflow-upgrade-20260909/office-native-drain-fullbuild-runtime.json)。已覆盖工作区选择停留 35 秒、插入、写回原文件与新会话重开；锚点/环绕调整、系统选择器和物理真机仍待验收。

- 2026-09-09 Excel 新导出运行失败已缩小到关联编号：独立应用 17 在 Notes!B5 插入后导出 1 个附件，保存重开后导出 0 个；ZIP 内 65,539 字节及 Unicode 文件名完整，17/18 项通过，但 shape ID 和 type 被错误转义。采用上游 VMLExport 的 legacy-ID 序列化方式修复；新 arm64 编译和 167 个其他成员保留校验通过，4 组独立关系图变异测试包含这两种转义错误。见 [运行失败](evidence/workflow-upgrade-20260909/office-excel-vml-id-runtime-failure.json) 与 [修复编译](evidence/workflow-upgrade-20260909/office-excel-vml-id-fix-compile.json)。实际修复重开尚未验收，A04/F04 保持未通过。

- Excel 关联编号修复取得实际通过结果：宿主 34339384837 的原生框架/全部资源校验通过；独立应用 18 的安装二进制匹配签名包。Notes!B5 插入后导出、保存关闭、只读重开、再次导出成功，两次 65,539 字节原哈希一致，工作表/VML/type/image/锚点等 29 项检查通过。见 [运行证据](evidence/workflow-upgrade-20260909/office-excel-vml-fixed-runtime.json) 与 [结构检查](evidence/workflow-upgrade-20260909/office-xlsx-vml-fixed-structure.json)。仅证明该实际样本，A04 的移动/尺寸/随单元格行为及完整原文件界面仍待验。

- 新组件的撤销重做保存仍失败：应用 18 第二个独立样本中附件数量为撤销 0、重做 1、重开 1，导出字节一致，但 XLSX 的 progId 为空，结构检查未通过。此前直接保存的 29 项通过结果不替代这个样本。新修复继承原生 SdrUndoNewObj，在原生 Redo 后使用当前 PersistName 恢复 Package 元数据；公开头文件实际语法检查、arm64 对象编译及 3 项宿主工程检查通过，待新宿主链接和运行复测。见 [失败证据](evidence/workflow-upgrade-20260909/office-excel-redo-metadata-failure.json)。

- Excel 重做元数据修复已通过实际运行：宿主 34340935668 校验并锁定；独立应用 19 撤销数量 0、重做 1，保存关闭重开后附件类型为 Package。继续编辑同一工作簿 C10 后第二次保存，类型及原字节仍正确；两次 30/30 检查、三次附件导出一致。见 [运行证据](evidence/workflow-upgrade-20260909/office-excel-redo-fixed-runtime.json)。同时未改附件的左位置 16.99→16.39 mm、宽 60.00→58.15 mm、高 18.00→17.99 mm，默认列宽 8.6796875→8.37109375；[几何失败证据](evidence/workflow-upgrade-20260909/office-xlsx-repeated-save-geometry-failure.json) 保持为 A04/F04 阻断项。原生导入优先按整数像素 VML 锚点和当前列宽重建，列宽变化的完整根因尚未确认，不将内容通过误标为位置保真。

- 参考设备实际修复：34348631751 的 Kit 导入与导出均使用虚拟参考设备，独立应用 22 两次保存重开后各表列宽不再缩小；附件撤销重做、三次导出字节一致，两份保存各 29 项结构检查通过。见 [运行记录](evidence/workflow-upgrade-20260909/office-xlsx-kit-device-runtime.json)。未修改的附件位置与页面锚定稳定，但宽/高仍减少 0.05/0.01 mm，A04/F04 精确保真保持未完成；PPT 图表数据、完整 Office 功能及物理设备验收未被此样本替代。

- 三锚定模式实际验证：34350509488 的固定页面与仅移动样本经两轮文字修改、保存关闭重开后，原位置/60×18 mm 尺寸/列宽及模式保持一致；随单元格缩放样本仍首次变为 59.95×17.99 mm。六份保存各 29 项附件结构检查与六次原字节导出通过，详见 [三模式运行记录](evidence/workflow-upgrade-20260909/office-xlsx-precise-ole-runtime.json)。这只验证原文件的模式保持和文字修改保存，不代表已经实测行列调整触发的移动/缩放、对象拖动/旋转或物理设备。A04/F04 保持未完成。

- PPT 数据引用补充检查：原始样本 5 项检查通过，实际保存文件同时缺少内嵌工作簿，且原 Sheet1!$B$1、Sheet1!$A$2:$A$3、Sheet1!$B$2:$B$3 变成 label 0、categories、0，已经不是 Excel 单元格引用。见 [源文件检查](evidence/workflow-upgrade-20260909/office-ppt-chart-source-data.json) 与 [保存失败检查](evidence/workflow-upgrade-20260909/office-ppt-chart-saved-data-failure.json)。新增只读语义验证器和 4 项故障注入测试：正确数据、旧工作簿、缺失载荷、错误引用；修复需要保留原数据文件并将编辑后的序列与实际单元格/公式对应，不能只补 relationship。

截图集中索引：[Office 截图与文案素材](OFFICE_SCREENSHOT_INDEX.md)。最终版本复拍、iPhone/iPad 和完整功能操作图继续随验收补充。
