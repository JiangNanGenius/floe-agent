# 本轮测试版代码与操作逻辑审计

用户最新发布要求：尽快交付 CI / TestFlight，先完成全部变更及直接调用链的代码审计，保证逻辑和操作流程自洽；不等待特别完整的手工测试。未测功能、已知限制及完整升级剩余项继续跟踪，不把测试版发布等同全部验收完成。

审计起点：`v1.5.3`；初始候选 `b54a2a3`，当前工作树修复还需纳入最终提交。差异含 455 个文件，包含代码、生成目录、截图与证据。仅查看文件名或运行测试不算完成代码审计。

## 当前确认与修复

- Office 原文件提交：已检查共享会话的只读/全屏切换、保存、另存、恢复副本与原子替换。发现 PPT 原生导出丢失图表数据后仍可能提交覆盖原文件。
- 已接入 `OfficeNativeSaveValidation`：普通 PPTX 图表必须保留数据关系及非空嵌入载荷，拒绝内部序列标识被当作 Excel 引用；保存先保留恢复副本再校验，另存也校验同一快照。损坏导出不会被当成成功文件交付。此保护不等于图表导出保真修复，也不验证全部图表类型和工作簿语义。
- 实际生产校验代码在隔离 Swift 检查程序中编译并运行：原始 PPTX 通过，已保留的真实损坏原生导出返回预期 `validationFailed`，2 项通过。新增仓库测试覆盖关系前缀、缺失载荷、内部引用以及原文件/恢复副本/另存一致性，完整应用测试仍待 CI。
- 原生依赖进入正式构建的链路已检查：`bootstrap_python_runtime.sh` 调用 Office 产物校验安装，项目配置对设备链接并嵌入，签名流程重签框架。没有将仅组件构建当成新 TestFlight。
- 动态任务清单已检查持久事务、修订 CAS、相同工具调用去重、稳定 ID 与取消/新清单约束；模型提供的 evidence 不被当作独立验证或 Goal 授权。

## 审计仍在进行

- 聊天分页、实时状态与历史合并：已修复最新快照与旧片段之间缺页游标丢失。消息及事件分别记录实际页覆盖区间，先补最近缺口再读更早历史；重新加载保留已有历史。会话快照直接查询最新 51 条事件，避免缓存水位跨越千条后仍停在旧尾部。4 项生产代码页区间测试执行通过；新增 240 条消息/事件的应用层加载—重连—分页测试待云端运行。滚动跟随调用链已检查：内容增长不取消跟随，上滑阅读取消，回到底部恢复；真实设备滚动位置与动画验收仍开放。
- 生图：供应商/模型/参数目录、实际编码、开关、预设优先级和 fallback；已阅读路由主体，适配器和全部调用方还需闭环复核。
- 工具/Skills/提示词：目录、按需加载、多查询、归属、动态计划与目标的实际注入，描述必须与执行一致。
- 文件/PDF/转换/恢复、插件生命周期、画布/素材、批量操作、后台/PiP、数据库迁移与其余变更逐项复核。
- 签名上传与 Apple/TestFlight 最终状态；当前 CI `34356037150` 基于 `b54a2a3`，不覆盖后续保存保护。最终提交必须另行通过 CI / 发布门槛。

## 证据与后续更新

- [Office 截图索引](OFFICE_SCREENSHOT_INDEX.md)
- [原始 PPT 图表数据](evidence/workflow-upgrade-20260909/office-ppt-chart-source-data.json)
- [原生保存失败数据](evidence/workflow-upgrade-20260909/office-ppt-chart-saved-data-failure.json)
- [完整升级台账](WORKFLOW_UPGRADE_IMPLEMENTATION.md)

当前审计尚未完成，不能作为“所有代码已经通过”的声明。完成各分组后更新结论、代码提交和必要检查证据，再锁定发布提交。
