# 本轮测试版代码与操作逻辑审计

用户最新发布要求：尽快交付 CI / TestFlight，先完成全部变更及直接调用链的代码审计，保证逻辑和操作流程自洽；不等待特别完整的手工测试。未测功能、已知限制及完整升级剩余项继续跟踪，不把测试版发布等同全部验收完成。

审计起点：`v1.5.3`；初始候选 `b54a2a3`，当前工作树修复还需纳入最终提交。差异含 455 个文件，包含代码、生成目录、截图与证据。仅查看文件名或运行测试不算完成代码审计。

## 当前确认与修复

- Office 原文件提交：已检查共享会话的只读/全屏切换、保存、另存、恢复副本与原子替换。发现 PPT 原生导出丢失图表数据后仍可能提交覆盖原文件。
- 已接入 `OfficeNativeSaveValidation`：普通 PPTX 图表必须保留数据关系及非空嵌入载荷，拒绝内部序列标识被当作 Excel 引用；保存先保留恢复副本再校验，另存也校验同一快照。损坏导出不会被当成成功文件交付。此保护不等于图表导出保真修复，也不验证全部图表类型和工作簿语义。
- 实际生产校验代码在隔离 Swift 检查程序中编译并运行：原始 PPTX 通过，已保留的真实损坏原生导出返回预期 `validationFailed`，2 项通过。新增仓库测试覆盖关系前缀、缺失载荷、内部引用以及原文件/恢复副本/另存一致性，完整应用测试仍待 CI。
- 原生依赖进入正式构建的链路已检查：`bootstrap_python_runtime.sh` 调用 Office 产物校验安装，项目配置对设备链接并嵌入，签名流程重签框架。没有将仅组件构建当成新 TestFlight。
- 动态任务清单已检查持久事务、修订 CAS、相同工具调用去重、稳定 ID 与取消/新清单约束；模型提供的 evidence 不被当作独立验证或 Goal 授权。

## 已审阅范围与验证边界

- 聊天分页、实时状态与历史合并：已修复最新快照与旧片段之间缺页游标丢失。消息及事件分别记录实际页覆盖区间，先补最近缺口再读更早历史；重新加载保留已有历史。会话快照直接查询最新 51 条事件，避免缓存水位跨越千条后仍停在旧尾部。4 项生产代码页区间测试执行通过；新增 240 条消息/事件的应用层加载—重连—分页测试待云端运行。滚动跟随调用链已检查：内容增长不取消跟随，上滑阅读取消，回到底部恢复；真实设备滚动位置与动画验收仍开放。
- 生图：已审阅目录、开关持久化/迁移、普通聊天与画布调用方、预设路由和适配器编码改动。发现 `image.models` 输出在工具和执行层被 4,096 字截断，已在两层保留完整的有界目录，并拒绝超预算页、提示减小 limit，避免返回残缺 JSON；新增两层回归待 CI。Fallback 仅允许明确的 401/404/429 拒绝，最多切换一次；超时、取消、服务器错误和未知结果不重发。真实供应商请求验收仍开放。
- 工具/Skills/提示词：已审閱本轮目录、多查询、安装升级、工具注册/结果传递、云端与本地提示词、记忆/Skill 辅助生成及 Goal 证据审查改动。底层能力与相关指南分开标注；任务清单不授权 Goal；无工具模型不注入可执行工具承诺。内置升级限定更高版本且未经用户改写的官方来源。还需最终生成 Skills 目录与发布包一致性检查。
- 已审阅原生 Office 控制器、附件导入/枚举/导出、恢复日志及锁、文件预览全屏切换、素材缩略图和后台状态本轮改动。附件导入在对象创建后元数据失败可能留下部分修改，已使前端明确提示先检查/撤销，保留编辑副本，不把失败误报为无修改；原生插入异常自动回滚仍是后续完善项。
- 文档恢复与附件选择界面、原生公开控制器和附件实现、正式 App 嵌入/摘要校验脚本已补阅；原生过滤器补丁、其余构建/打包脚本、直接调用链和最终资源生成物还需完成最终审阅；不能据此宣布全量审计完成。
- 签名上传与 Apple/TestFlight 最终状态；生产代码候选 `14b4f13`（包含保存保护、分页、目录完整性和附件失败反馈），已推送并提交 CI `34359227808`。旧候选 CI `34356037150` 的 Linux 与 build-test 已成功，App Store SDK 构建仍进行中；旧排队任务 `34358549140` 已请求取消，以最新候选替代，避免浪费重复构建。最终提交必须另行通过 CI / 发布门槛。

## 证据与后续更新

- [Office 截图索引](OFFICE_SCREENSHOT_INDEX.md)
- [原始 PPT 图表数据](evidence/workflow-upgrade-20260909/office-ppt-chart-source-data.json)
- [原生保存失败数据](evidence/workflow-upgrade-20260909/office-ppt-chart-saved-data-failure.json)
- [完整升级台账](WORKFLOW_UPGRADE_IMPLEMENTATION.md)

2026-09-09 收尾：上文的剩余代码审阅已完成，包括原生生命周期与 XLSX 过滤器补丁、构建输入补全、打包/重定位/摘要校验及发布工作流。只替换锁定的原生归档成员，ABI 相关分配方同步重编译，资格应用不会被当作正式发布证据。

本轮变更及直接调用链的代码审阅完成，不代表整个上游 Office 引擎每一行已审计，也不等于全部功能或布局保真已通过运行验收。当前未发现尚未处理且必须阻止测试版构建的新增问题；PPT 图表保存问题以保存保护和明确限制处理，根本修复保留在完整计划。最终发布提交仍须通过云端编译、测试、签名及 Apple 处理。

必要检查：Python 3.13 下 Office 契约检查 66/66 通过，见 `evidence/workflow-upgrade-20260909/release-office-code-contract-tests.log`。首次使用系统旧 Python 不支持 tarfile 的 filter 参数，切换与 CI 对应的 Python 后通过，没有跳过测试。`skill-hub/build.py --check` 校验官方包、签名和生成定义通过，4 项目录测试通过。旧候选 CI 34356037150 已全部成功，最新生产代码 CI 34359227808 运行中。

发布版本选择为 1.6.0（135）：远端标签与 Apple 查询确认最新上传仍为 1.5.3（134）。测试组可见性尚待新包上传后核实。
