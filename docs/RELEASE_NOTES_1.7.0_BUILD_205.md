# Floe Agent 1.7.0 (205) — internal test candidate

**Not uploaded yet.** Build 205 metadata is prepared on the feedback integration
branch. The immutable source tag and cloud run will be recorded when dispatched.

This candidate carries the September 20 Office lifecycle, internal IDE document
tabs, local Git initialization, native mind-map and Shell/session repairs.
The [repair record](FLOE_FEEDBACK_2026_09_20.md) distinguishes implementation,
focused host evidence and outstanding device checks. Cross-task, local-model and new Linux/package changes are excluded from this
frozen delivery and remain assigned for the next candidate. The incomplete
cross-task patch was reverted on this release branch to avoid its known
pagination regression; the integration branch retains it for correction.

The user explicitly requested lightweight verification and TestFlight delivery
for manual device testing. This delivery uses the accepted-SDK cloud App build
and preserves the exact-source IPA and private symbols before signing. Simulator
and UI qualification are skipped. An uploaded build is called installable only
after Apple processing and existing Floe QA group availability are verified.

本候选已统一 App 和三个扩展的构建号为 205，尚未上传。包含 Office 生命周期、
IDE 内部 PDF／Office 标签、本地 Git 初始化、原生思维导图和 Shell 修复。
跨任务、本地模型与 Linux／安装入口改动不属于本包，仍继续处理。详细验证边界见
[反馈修复记录](FLOE_FEEDBACK_2026_09_20.md)。

按用户要求，只做定向轻量检查和云端 App 编译，不做反复界面回归。真机效果
由用户验收；上传接受、Apple 处理完成及内部组可安装会分别记录。
