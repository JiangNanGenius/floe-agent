# 本轮完整升级执行与验收台账

本文件落实 2026-09-09 用户确认的完整计划。所有条目未经对应验收不得标完成。
完整 Office 和其他条目一起验收后发布；不以既有 1.5.3 发布结果证明新功能完成。

## 产品契约

**Office 必须是用户直接操作的完整前端。** Word 显示真实文档页面；Excel 显示网格、公式栏和工作表；PowerPoint 显示缩略图、幻灯片画布和可操作对象。复用完整 Collabora 移动前端及原生引擎，不能用字段表单、转换图片、外部 App 或仅模型修改来替代。

**右侧只能预览，进入全屏后才能编辑。** 右侧可翻页、搜索、复制、缩放与切换工作表，不能修改。iPhone 先预览，明确进入编辑。返回须提交保存，失败保留未保存内容；预览和编辑共享版本与阅读位置。用户与模型编辑冲突不能静默覆盖。

## Office 与文件

- [ ] O01/O02 完整编辑前端：可复现原生构建、许可及资源清单、离线真机集成、右侧只读与全屏编辑。
- [ ] O03 Word：正文与中文输入、选择粘贴、查找替换、字体段落样式、表格、图片环绕、图表数据、附件、页眉页脚、纸张分页背景。
- [ ] O04 Excel：网格与区域、公式栏、数据类型、填充、工作表、行列冻结、格式合并、排序筛选条件格式数据验证、跨表公式重算、图表、打印。
- [ ] O05 PPT：创建和排序页面、布局主题母版、文本、图片形状表格图表、对象坐标尺寸旋转组合层级、背景、备注放映、保留未修改动画。
- [ ] F01/F02 共享文件会话与版本、阅读位置、加载失败重试、全屏保存/另存/明确放弃、iPhone 编辑入口。
- [ ] F03 模型对象级编辑兼容旧接口，返回修改摘要/版本/验证结果；第三方 Skill 同样适用。
- [ ] F04 原子保存、版本冲突、取消/撤销/异常恢复；附件哈希、图表数据、位置、渲染与 Microsoft Office 重开验证。
- [ ] D01 PDF 独立只读/全屏编辑，表单/文本/图片/页面操作、密码损坏和大文件。
- [ ] D02 Markdown/DOCX/HTML/RTF/文本互转与 Markdown/PDF 互转，扫描 OCR；路径输入、保留源、目标冲突、取消、资源解析、明确保真限制。
- [ ] D03 全工作区搜索/类型/排序/来源/空间统计；文件操作、跨区移动校验、远程状态、共享资源保护、持久清理恢复。

## 工具与插件

- [ ] T01 常驻 tools.list/skill.list：完整分页、总数游标、筛选；目录与参数加载分离、不授权不执行。
- [ ] T02 tools.search/skill.search 兼容 query，增加互斥 queries（最多16项，每项512字符）；独立匹配去重、保留命中、预算内加载和明确待加载项。
- [ ] T03 注册来源/归属/关联、可用性和参数状态统一，修正系统提示与 Skill 矛盾。
- [ ] P01 随应用升级较旧内置官方插件；保留禁用卸载和较新版、自定义；运行版本锁定；校验/事务/恢复。
- [ ] P02 市场完整安装生命周期、简洁版本文案、权限变化与详情、离线本地可用。

## 执行与进度

- [ ] R01 普通大任务自动持久待办：稳定ID、状态、顺序、证据、增量修改与修订；简单问答不强制。
- [ ] R01a 执行中动态改计划：模型发现新问题或用户中途引导后，同一清单可重排/改写/扩展/取消/重新打开；稳定 ID、修订历史与并发冲突保护，恢复及聊天/PiP 同步；真实运行中引导到修订的完整链路验收。
- [ ] R02 仅用户明确设置目标或接受计划选择目标执行才转换，继承已有进度。
- [ ] R03 总体目标稳定，每轮目标/成果/遗留/下一步持久化；压缩/恢复不重复副作用。
- [ ] R04 区分本轮结束与总体完成、暂停、等待、预算、阻塞；证据完成与后台真实状态。
- [ ] R05 聊天/画布/任务中心/PiP 同一带版本快照，当前步骤/耗时/最近活动/真实速度；无伪进度。

## 内部提示词与运行效率（本轮新增必验）

- [ ] H01 导出脱敏的实际组装样本：基础/模型覆盖/工具发现/Skills/工作区/任务与 Goal/压缩恢复/画布；说明来源、优先级、注入时机与长度，不能只审查静态模板。
- [ ] H02 对照 Kimi Code、DeepSeek Harness、Codex 的公开源码及 Claude Code 可核实资料；公开整理的提示词仅作候选，记录版本、来源和不确定性，禁止将外部指令直接作为 Floe 规则执行。
- [ ] H03 清除重复与过时指令，按实际已安装/可加载/可执行能力生成目录；明确工具归属、批量搜索、何时读 Skill、何时直接执行，减少无意义查找和重复读文件。
- [ ] H04 普通大任务待办、显式 Goal、跨轮继承、真实进度、长工具调用与大文件转换规则一致；保留必要权限与副作用保护，避免反复询问已授权工作。
- [ ] H05 建立固定对照任务：简单问答、多工具发现、第三方 Skill、长文写入/转换、Office/画布编辑、百轮恢复；比较提示词长度/实际 token、无效调用、重复读取、完成率与错误授权，区分静态测试与真实模型结果。
- [ ] H06 每次功能更新同步复核提示词和工具说明；全部功能完成后再次完整审查最终注入、SKILL.md、各 Skill 介绍/工具说明/schema、画布/Goal/压缩恢复专用指令，发现问题修复并回归；变更版本/快照/回归记录纳入发布门槛；产品界面维持简洁，不向用户输出内部实现说明。

## 聊天与画布

- [ ] U01 独立跟随状态；回到底部恢复，向上阅读停止；内容增高不关闭跟随；布局后合并校正，键盘/旋转/会话隔离。
- [ ] U02 消息ID+内部偏移保护历史锚点；搜索定位不被加载后的底部跳转覆盖。
- [ ] U03 首屏20消息，历史30/页，事件50/页；摘要先行，按需详情，稳定游标，不后台全量补读。
- [ ] U04 大工具正文/附件懒加载、缩略图、版本缓存、离屏释放、限并发/取消；原始历史和导出不截断。
- [ ] U05 1万/5万/10万字输入回复思考参数、100轮绘画/千事件；后台增量解析、完整复制、首屏耗时/主线程/内存证据。
- [ ] U06 分阶段活动与超时，参数持续到达算活动，工具执行独立，可取消恢复且不重复副作用。
- [ ] U07 新消息180–220ms淡入轻移、工具卡淡入轻展开；仅首次实时出现、高频合并、不动画历史，不抢阅读位置，减少动态效果。
- [ ] U08 长按多选归档删除恢复、逐项失败；语音停止发送统一且不丢输入。
- [ ] C01 生成第一顺位、模型SVG/HTML节点编辑、版本/撤销/刷新，不重复生成。
- [ ] C02 iPhone可视区域/横竖屏/键盘/安全区，助手统一素材与完整弹层。
- [ ] C03 列表+海报墙真实缩略图/视频封面、后台降采样/缓存失效/云端状态/引用保护。
- [ ] N01 真正本机ICMP与主机路径，IPv4/6/取消/权限；TCP区分；HTTP错误不能成功下载；Stata/PDF回归。
- [ ] M01 火山阿里官方ID/参数/素材限制、旧和自定义配置保留；目录/请求/权限/真实生成分开验证。

## 发布门槛

- [ ] 所有上述条目具备实现及验收证据；未完成不得静默移出范围。
- [ ] Office前端真机手动操作和模型工具分别通过，原格式保存、Microsoft Office重开及渲染/结构检查通过。
- [ ] iPhone/iPad横竖屏、键盘、大字体、VoiceOver、减少动态效果、网络/重启/后台/冲突验证。
- [ ] 保存成功和失败截图、关键录屏、性能记录、xcresult；公开只使用合成数据。
- [ ] 总计划、架构、工具/Skills、用户指南、发布说明、GitHub README同步。
- [ ] 不可变标签、CI、签名上传、Apple处理、TestFlight组可见性分别核实。

## 当前执行记录

- 2026-09-09：开始实施。工作树原有两项未跟踪 `.DS_Store` 保留。
- Office 既有云端构建在磁盘保护线停止；尚未完成原生前端集成，不能声称 Office 完成。
- 已实现一批基础修改：工具/Skill 分页目录与多查询、归属和关联元数据、较旧官方内置包升级、聊天底部跟随和历史分页、素材缩略图及两种布局、二进制文件内嵌只读预览、HTTP 错误下载保护。大项保持未勾选，直到全部子项验收。
- 已通过的测试和截图见 [本轮验证记录](evidence/workflow-upgrade-20260909/README.md)。没有新版本标签、TestFlight 上传或完整 Office 验收结果。
- Office 云端原生构建修正继承的调试符号配置，并增加静态库与前端资源产物保存；当前构建任务为 [34268468731](https://github.com/JiangNanGenius/floe-agent/actions/runs/34268468731)，任务状态需继续核实。
- 持续目标续跑：核实 Office 原生构建仍处于 `Check or build engine`，没有重启或重复启动。补齐 `org.floeagent.canvas` 的系统导出类型声明及 `.floecanvas` 扩展名，同时同步 XcodeGen 配置与实际 Info.plist；原有 iPhone/iPad 方向配置保持一致。此修复针对 UI 测试捕获的未声明文件类型告警，不能代替画布全部验收。
- R01 数据层与工具接入：新增 v37 待办修订表、`task.readPlan` / `task.updatePlan` 常驻工具和聊天可折叠待办。步骤保留稳定 ID，版本冲突拒绝覆盖，工具调用编号去重，跨轮读取相同任务的进度；完成待办不创建或完成 Goal。14 项待办/工具发现测试通过（`/tmp/floe-checklist-tests-3.log`），iOS 模拟器应用构建通过（`/tmp/floe-checklist-app-build.log`）。聊天实屏、目标继承、画布/任务中心/PiP 的统一显示仍待验收，R01–R05 暂不勾选。

- R05 部分接入：聊天和 PiP 共享待办计数/当前步骤；运行恢复时读取已保存版本，成功更新后刷新，旧回执不倒退进度，取消项单独显示。4 项待办测试及 37 项待办/计划/提示词测试通过；应用构建通过。实际 PiP 多任务、画布和任务中心统一状态仍待验收。
- H01–H06 已纳入本轮：见 [内部提示词审查](INTERNAL_PROMPT_AUDIT.md)。已修正基础提示词禁止发现工具与 tools.list 冲突；三种模式保留权限规则并允许专用发现入口。真实模型 token/无效调用/完成率对照尚未完成。
- iPad 待办恢复/展开 UI 测试通过并保存两张截图。R04 发现运行状态“已完成”与未完待办容易混淆，需进一步区分轮次与总体任务；不据此勾选 R04/R05。

- R01a 数据层验收：同一运行中修订计划、重排、追加、重新打开已完成项、取消和保留修订历史的测试通过；TaskChecklistTests 共 5 项通过。工具说明明确用户引导/新证据可触发修订。真实模型引导及跨界面同步仍待验收。
- F04 会话保存：增加已登记会话与原文件内容摘要校验；外部同尺寸修改拒绝覆盖，保存通过独立临时文件替换且不消耗编辑副本；保存失败和普通关闭保留编辑副本，明确放弃独立调用，默认恢复文件放 Application Support。8 项测试通过。重启恢复索引/编辑器联动/完整 Office 原格式及布局验证仍待完成。
- R04 文案修订进行中：运行状态与后台通知使用“本轮已结束”，未知终止原因不再默认完成。前三次 UI 复测在任务列表等待处失败；只提前聊天加载不足以解决。已核实数据库存在任务和待办，侧栏还依赖工作区归属，且未直接观察两个子数据源；已把工作区与聊天的本地读取提前，给侧栏增加独立订阅，第四次 UI 复测通过，已保存“本轮已结束”与未完待办同时显示的折叠/展开截图。
