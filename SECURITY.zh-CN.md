# 安全策略

[English](SECURITY.md) · [中文 README](README.zh-CN.md)

## 当前状态

Floe Agent 提供用于评估的预发布版本，但不是受支持的生产服务。社区未签名 IPA 不是 App Store 安装包。不要使用开发版或未签名版本操作生产系统或保存生产凭据。

## 报告漏洞

请勿在公开 Issue 中发布可利用细节。进入仓库的 **Security** 页面，选择 **Report a vulnerability** 创建私有 GitHub Security Advisory。

报告应包含：

- 受影响的提交、分支或版本；
- 组件和运行环境；
- 复现步骤或最小 PoC；
- 预期与实际安全边界；
- 凭据、隐私数据或远程主机是否可能暴露的证据。

在调查和协调披露方案完成前，请保持报告私密。

## 核心安全边界

- 模型 API 凭据保存在 Keychain，不写入日志或 SQLite。
- SSH 凭据和主机密钥默认只保存在设备本地；主机密钥异常变化必须失败关闭。
- 模型输出、远程内容、工具参数和 Skill 包全部是不可信输入。
- Provider 只看到任务允许的工具，执行器仍会独立校验参数、作用域和权限。
- 全权控制必须显式授权、限时且可撤销；灾难性操作仍需要单独确认。
- 经 SSH 隧道的 VNC 是安全默认值。普通 VNC 仅作为用户主动选择的旧设备连接方式；Floe 会在使用前提示风险，并把密码保存在 Keychain。
- 任意下载或模型生成的代码不会在 iOS 本机执行。已安装 Skill 只有在创建或安装时完成一次审计后，才能附带纯 Python 脚本；后续免重复审批的复用必须匹配同一不可变内容指纹和作用域。
- 浏览器引用与文档绑定；登录、上传、凭据和支付流程必须由用户复核或接管。
- Skill 必须经过静态验证，不能动态注册原生执行器或自行获得权限。随 Skill 提供的纯 Python 辅助脚本仍在沙箱内运行，不继承远程、凭据或文件系统权限。

这些控制不能让无限制的远程 Shell 或图形控制变得绝对安全。用户仍需对所选主机、凭据、备份、服务商条款及自己批准的操作负责。
