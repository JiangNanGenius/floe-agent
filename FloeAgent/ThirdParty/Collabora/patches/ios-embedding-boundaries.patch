--- a/ios/Mobile/DocumentViewController.mm
+++ b/ios/Mobile/DocumentViewController.mm
@@ -15,8 +15,7 @@
 #import <string>
 #import <vector>
 
-#import <objc/message.h>
-#import <objc/runtime.h>
+#import <GameController/GameController.h>
 
 #import <poll.h>
 #import <sys/stat.h>
@@ -39,52 +38,37 @@
 @interface DocumentViewController() <WKNavigationDelegate, WKUIDelegate, WKScriptMessageHandler, UIScrollViewDelegate, UIDocumentPickerDelegate, UIFontPickerViewControllerDelegate> {
     int closeNotificationPipeForForwardingThread[2];
     NSURL *downloadAsTmpURL;
+    BOOL floeClosing;
+    BOOL floeClosed;
 }
 
 @end
 
-// From https://gist.github.com/myell0w/d8dfabde43f8da543f9c
+// Floe uses public keyboard discovery. WebKit retains its normal requirement
+// for user interaction when focusing an editable element.
 static BOOL isExternalKeyboardAttached()
 {
-    BOOL externalKeyboardAttached = NO;
-
-    @try {
-        NSString *keyboardClassName = [@[@"UI", @"Key", @"boa", @"rd", @"Im", @"pl"] componentsJoinedByString:@""];
-        Class c = NSClassFromString(keyboardClassName);
-        SEL sharedInstanceSEL = NSSelectorFromString(@"sharedInstance");
-        if (c == Nil || ![c respondsToSelector:sharedInstanceSEL]) {
-            return NO;
-        }
-
-#pragma clang diagnostic push
-#pragma clang diagnostic ignored "-Warc-performSelector-leaks"
-        id sharedKeyboardInstance = [c performSelector:sharedInstanceSEL];
-#pragma clang diagnostic pop
-
-        if (![sharedKeyboardInstance isKindOfClass:NSClassFromString(keyboardClassName)]) {
-            return NO;
-        }
-
-        NSString *externalKeyboardSelectorName = [@[@"is", @"InH", @"ardw", @"areK", @"eyb", @"oard", @"Mode"] componentsJoinedByString:@""];
-        SEL externalKeyboardSEL = NSSelectorFromString(externalKeyboardSelectorName);
-        if (![sharedKeyboardInstance respondsToSelector:externalKeyboardSEL]) {
-            return NO;
-        }
-
-        externalKeyboardAttached = ((BOOL ( *)(id, SEL))objc_msgSend)(sharedKeyboardInstance, externalKeyboardSEL);
-    } @catch(__unused NSException *ex) {
-        externalKeyboardAttached = NO;
-    }
-
-    return externalKeyboardAttached;
-}
+    return [GCKeyboard coalescedKeyboard] != nil;
+}
+
+// FLOE_READONLY_HANDSHAKE_BEGIN
+static NSURL *FloeDocumentHandshakeURL(NSURL *fileURL, BOOL readOnly) {
+    NSURLComponents *components = [NSURLComponents componentsWithURL:fileURL resolvingAgainstBaseURL:NO];
+    NSMutableArray<NSURLQueryItem *> *items = [NSMutableArray array];
+    for (NSURLQueryItem *item in components.queryItems ?: @[])
+        if (![item.name isEqualToString:@"permission"]) [items addObject:item];
+    if (readOnly) [items addObject:[NSURLQueryItem queryItemWithName:@"permission" value:@"readonly"]];
+    components.queryItems = items.count ? items : nil;
+    return components.URL;
+}
+// FLOE_READONLY_HANDSHAKE_END
 
 @implementation DocumentViewController
-
-static IMP standardImpOfInputAccessoryView = nil;
 
 - (void)viewDidLoad {
     [super viewDidLoad];
+    closeNotificationPipeForForwardingThread[0] = -1;
+    closeNotificationPipeForForwardingThread[1] = -1;
 
     WKWebViewConfiguration *configuration = [[WKWebViewConfiguration alloc] init];
     WKUserContentController *userContentController = [[WKUserContentController alloc] init];
@@ -129,61 +113,8 @@
     self.webView.navigationDelegate = self;
     self.webView.UIDelegate = self;
 
-    // Hack for tdf#129380: Don't show the "shortcut bar" if a hardware keyboard is used.
-
-    // From https://inneka.com/programming/objective-c/hide-shortcut-keyboard-bar-for-uiwebview-in-ios-9/
-    Class webBrowserClass = NSClassFromString(@"WKContentView");
-    Method method = class_getInstanceMethod(webBrowserClass, @selector(inputAccessoryView));
-
-    if (isExternalKeyboardAttached()) {
-        IMP newImp = imp_implementationWithBlock(^(id _s) {
-                if ([self.webView respondsToSelector:@selector(inputAssistantItem)]) {
-                    UITextInputAssistantItem *inputAssistantItem = [self.webView inputAssistantItem];
-                    inputAssistantItem.leadingBarButtonGroups = @[];
-                    inputAssistantItem.trailingBarButtonGroups = @[];
-                }
-                return nil;
-            });
-
-        IMP oldImp = method_setImplementation(method, newImp);
-        if (standardImpOfInputAccessoryView == nil)
-            standardImpOfInputAccessoryView = oldImp;
-    } else {
-        // If the external keyboard has been disconnected, restore the normal behaviour.
-        if (standardImpOfInputAccessoryView != nil) {
-            method_setImplementation(method, standardImpOfInputAccessoryView);
-        }
-
-        // Hack to make the on-screen keyboard pop up more eagerly when focus set to the textarea
-        // using JavaScript.
-
-        // From https://stackoverflow.com/questions/32449870/programmatically-focus-on-a-form-in-a-webview-wkwebview/32845699
-
-        static bool doneThisAlready = false;
-        if (!doneThisAlready) {
-            const char * methodSignature;
-            doneThisAlready = true;
-
-            if ([[NSProcessInfo processInfo] isOperatingSystemAtLeastVersion: (NSOperatingSystemVersion){13, 0, 0}]) {
-                methodSignature = "_elementDidFocus:userIsInteracting:blurPreviousNode:activityStateChanges:userObject:";
-            } else {
-                methodSignature = "_elementDidFocus:userIsInteracting:blurPreviousNode:changingActivityState:userObject:";
-            }
-
-            // Override that internal method with an own wrapper that always passes the
-            // userIsInteracting parameter as TRUE. That will cause the on-screen keyboard to pop up
-            // when we call the focus() method on the textarea element in JavaScript.
-            SEL selector = sel_getUid(methodSignature);
-            Method method = class_getInstanceMethod(webBrowserClass, selector);
-            if (method != nil) {
-                IMP original = method_getImplementation(method);
-                IMP override = imp_implementationWithBlock(^void(id me, void* arg0, BOOL arg1, BOOL arg2, BOOL arg3, id arg4) {
-                        ((void (*)(id, SEL, void*, BOOL, BOOL, BOOL, id))original)(me, selector, arg0, TRUE, arg2, arg3, arg4);
-                    });
-                method_setImplementation(method, override);
-            }
-        }
-    }
+    // Retain the standard UIKit keyboard accessory controls. Never swizzle
+    // WKContentView or force a private user-interaction flag process-wide.
 
     WKWebView *webViewP = self.webView;
     NSDictionary *views = NSDictionaryOfVariableBindings(webViewP);
@@ -257,18 +188,31 @@
 }
 
 - (IBAction)dismissDocumentViewController {
-    [[NSNotificationCenter defaultCenter] removeObserver:self name:UIKeyboardWillHideNotification object:nil];
-    [self dismissViewControllerAnimated:YES completion:^ {
-            [self.document closeWithCompletionHandler:^(BOOL success){
-                    LOG_TRC("close completion handler gets " << (success?"YES":"NO"));
-                    [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"debug"];
-                    [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"lok"];
-                    [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"error"];
-                    // Don't set webView.configuration.userContentController to
-                    // nil as it generates a "nil not allowed" compiler warning
-                    [self.webView removeFromSuperview];
-                    self.webView = nil;
-                    }];
+    if (floeClosing || floeClosed)
+        return;
+    floeClosing = YES;
+    // UIDocument must settle before the host dismisses the editor or writes
+    // its working file back to the original. Failed closes retain all copies.
+    [self.document closeWithCompletionHandler:^(BOOL success) {
+        dispatch_async(dispatch_get_main_queue(), ^{
+            self->floeClosing = NO;
+            if (!success) {
+                if (self.floeCloseCompletion)
+                    self.floeCloseCompletion(NO);
+                return;
+            }
+            self->floeClosed = YES;
+            [[NSNotificationCenter defaultCenter] removeObserver:self name:UIKeyboardWillHideNotification object:nil];
+            [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"debug"];
+            [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"lok"];
+            [self.webView.configuration.userContentController removeScriptMessageHandlerForName:@"error"];
+            [self.webView removeFromSuperview];
+            self.webView = nil;
+            if (self.floeCloseCompletion)
+                self.floeCloseCompletion(YES);
+            else
+                [self dismissViewControllerAnimated:YES completion:nil];
+        });
     }];
 }
 
@@ -429,7 +373,10 @@
 
             // First we simply send the Online C++ parts the URL and the appDocId. This corresponds
             // to the GET request with Upgrade to WebSocket.
-            std::string url([[self.document->copyFileURL absoluteString] UTF8String]);
+            // Native preview authority belongs to the backend session too. The
+            // HTML query alone only controls the initial browser presentation.
+            NSURL *sessionURL = FloeDocumentHandshakeURL(self.document->copyFileURL, self.document->readOnly);
+            std::string url(sessionURL.absoluteString.UTF8String);
 
             // This is read in the code in ClientRequestDispatcher::handleIncomingMessage()
             std::string message(url + " " + std::to_string(self.document->appDocId));
@@ -605,20 +552,19 @@
 }
 
 - (void)bye {
-    // Close one end of the socket pair, that will wake up the forwarding thread above
-    fakeSocketClose(closeNotificationPipeForForwardingThread[0]);
-
-    // DocumentData::deallocate(self.document->appDocId);
-
-    if (![[NSFileManager defaultManager] removeItemAtURL:self.document->copyFileURL error:nil]) {
-        LOG_SYS("Could not remove copy of document at " << [[self.document->copyFileURL path] UTF8String]);
-    }
-
-    // The dismissViewControllerAnimated must be done on the main queue.
-    dispatch_async(dispatch_get_main_queue(),
-                   ^{
-                       [self dismissDocumentViewController];
-                   });
+    dispatch_async(dispatch_get_main_queue(), ^{
+        if (self->floeClosing || self->floeClosed)
+            return;
+        // An open failure can reach BYE before HULLO created a pipe. Never
+        // close an uninitialized fake descriptor belonging to another editor.
+        if (self->closeNotificationPipeForForwardingThread[0] >= 0) {
+            fakeSocketClose(self->closeNotificationPipeForForwardingThread[0]);
+            self->closeNotificationPipeForForwardingThread[0] = -1;
+        }
+        // Floe owns cleanup after coordinated writeback or explicit discard.
+        // Preserve the engine copy even when WebKit terminates unexpectedly.
+        [self dismissDocumentViewController];
+    });
 }
 
 - (void)exportFileURL:(NSURL *)fileURL {
--- a/ios/Mobile/AppDelegate.mm
+++ b/ios/Mobile/AppDelegate.mm
@@ -136,17 +136,8 @@
     if (user_name == nullptr)
         user_name = [[[NSUserDefaults standardUserDefaults] stringForKey:@"userName"] UTF8String];
 
-    // Remove any leftover allegedly temporary folders with copies of documents left behind from
-    // previous instances of the app that were killed while editing, various random files that for
-    // instance NSS seems to love to create, etc, by removing the whole tmp folder.
-    NSURL *tempFolderURL = [[NSFileManager defaultManager] temporaryDirectory];
-    if (![[NSFileManager defaultManager] removeItemAtURL:tempFolderURL error:nil]) {
-        NSLog(@"Could not remove tmp folder %@", tempFolderURL);
-    }
-
-    if (![[NSFileManager defaultManager] createDirectoryAtURL:tempFolderURL withIntermediateDirectories:YES attributes:nil error:nil]) {
-        NSLog(@"Could not create tmp folder %@", tempFolderURL);
-    }
+    // Floe owns per-document recovery and cleanup. Initializing the Office
+    // engine must not delete other tasks' temporary files or pending exports.
 
     fakeSocketSetLoggingCallback([](const std::string& line)
                                  {
--- a/ios/Mobile/DocumentViewController.h
+++ b/ios/Mobile/DocumentViewController.h
@@ -20,6 +20,9 @@
 @property (strong) WKWebView *webView;
 @property void *schemeHandler;
 
+// Main-queue completion; host owns dismissal and cleanup when installed.
+@property (copy, nullable) void (^floeCloseCompletion)(BOOL success);
+
 - (void)bye;
 
 - (void)exportFileURL:(NSURL *)fileURL;
--- a/ios/Mobile/CODocument.h
+++ b/ios/Mobile/CODocument.h
@@ -13,7 +13,6 @@
 
 #import <UIKit/UIKit.h>
 
-#import <COKit/COKit.hxx>
 
 @class DocumentViewController;
 
@@ -27,6 +26,19 @@
 
 @property (weak) DocumentViewController *viewController;
 
+// Floe supplies a persistent session directory; every open creates a new child.
+@property (copy, nullable) NSURL *floeEngineCopyDirectory;
+
+// Main-queue event for each engine save. Success means UIDocument finished
+// writing fileURL, not that Floe committed the original or verified layout.
+@property (copy, nullable) void (^floeSaveCompletion)(BOOL success);
+
+// Join broker request identity with the matching native persistence result.
+// Sequence IDs identify engine results; they never imply original-file writeback.
+@property (copy, nullable) void (^floeSaveSequenceCompletion)(NSString *sequence, BOOL success);
+@property (copy, nullable) void (^floeSaveSequenceAssociation)(NSString *sequence, NSString *requestID);
+@property (copy, nullable) void (^floeSaveRequestRejected)(NSString *requestID);
+
 - (void)send2JS:(const char*)buffer length:(int)length;
 
 @end
--- a/kit/ChildSession.cpp
+++ b/kit/ChildSession.cpp
@@ -15,6 +15,7 @@
  */
 
 #include <config.h>
+#include <atomic>
 
 #include "ChildSession.hpp"
 
@@ -4243,7 +4244,7 @@
     {
         Parser parser;
         Poco::Dynamic::Var var = parser.parse(payload);
-        const Object::Ptr& object = var.extract<Object::Ptr>();
+        Object::Ptr object = var.extract<Object::Ptr>();
 
         auto commandName = object->get("commandName");
         auto success = object->get("success");
@@ -4265,27 +4266,39 @@
                 // After the document has been saved (into the temporary copy that we set up in
                 // -[CODocument loadFromContents:ofType:error:]), save it also using the system API so
                 // that file provider extensions notice.
-                if (!success.isEmpty() && success.toString() == "true")
-                {
 #if defined(IOS)
-                    CODocument* document =
-                        DocumentData::get(_docManager->getMobileAppDocId()).coDocument;
+                CODocument* document =
+                    DocumentData::get(_docManager->getMobileAppDocId()).coDocument;
+                const bool engineSaved = !success.isEmpty() && success.toString() == "true";
+                static std::atomic<unsigned long long> floeSequenceCounter(1);
+                const std::string floeSequence = std::to_string(floeSequenceCounter++);
+                object->set("floeSaveSequence", floeSequence);
+                NSString *floeSaveSequence = [NSString stringWithUTF8String:floeSequence.c_str()];
+                dispatch_async(dispatch_get_main_queue(), ^{
+                    if (!engineSaved) {
+                        if (document.floeSaveCompletion)
+                            document.floeSaveCompletion(NO);
+                        if (document.floeSaveSequenceCompletion)
+                            document.floeSaveSequenceCompletion(floeSaveSequence, NO);
+                        return;
+                    }
                     [document saveToURL:[document fileURL]
                          forSaveOperation:UIDocumentSaveForOverwriting
-                        completionHandler:^(BOOL success) {
-                          LOG_TRC("ChildSession::loKitCallback() save completion handler gets "
-                                  << (success ? "YES" : "NO"));
-                          if (![[NSFileManager defaultManager] removeItemAtURL:document->copyFileURL
-                                                                         error:nil])
-                          {
-                              LOG_SYS("Could not remove copy of document at "
-                                      << [[document->copyFileURL path] UTF8String]);
-                          }
+                        completionHandler:^(BOOL saved) {
+                          // Keep copyFileURL for further edits, retry and crash
+                          // recovery. Only Floe's session owner may remove it.
+                          dispatch_async(dispatch_get_main_queue(), ^{
+                              if (document.floeSaveCompletion)
+                                  document.floeSaveCompletion(saved);
+                              if (document.floeSaveSequenceCompletion)
+                                  document.floeSaveSequenceCompletion(floeSaveSequence, saved);
+                          });
                         }];
+                });
 #elif defined(__ANDROID__)
+                if (!success.isEmpty() && success.toString() == "true")
                     postDirectMessage("SAVE " + payload);
 #endif
-                }
             }
         }
 
@@ -4314,7 +4327,16 @@
             break;
         }
 
-        const std::string saveMessage = "unocommandresult: " + payload;
+        std::string floeForwardedPayload = payload;
+#if defined(IOS)
+        if (object->has("floeSaveSequence"))
+        {
+            std::ostringstream encoded;
+            object->stringify(encoded);
+            floeForwardedPayload = encoded.str();
+        }
+#endif
+        const std::string saveMessage = "unocommandresult: " + floeForwardedPayload;
         sendTextFrame(saveMessage);
         if (saveCommand)
             _docManager->handleSaveMessage(saveMessage);
--- a/ios/Mobile/CODocument.mm
+++ b/ios/Mobile/CODocument.mm
@@ -48,7 +48,18 @@
 
 @implementation CODocument
 
+- (instancetype)initWithFileURL:(NSURL *)url {
+    self = [super initWithFileURL:url];
+    if (self)
+        fakeClientFd = -1;
+    return self;
+}
+
 - (id)contentsForType:(NSString*)typeName error:(NSError **)errorPtr {
+    if (readOnly) {
+        if (errorPtr) *errorPtr = [NSError errorWithDomain:NSCocoaErrorDomain code:NSFileWriteNoPermissionError userInfo:nil];
+        return nil;
+    }
     return [NSData dataWithContentsOfFile:[copyFileURL path] options:0 error:errorPtr];
 }
 
@@ -65,24 +76,35 @@
     if (fakeClientFd >= 0)
         return YES;
 
-    fakeClientFd = fakeSocketSocket();
-
     appDocId = appDocIdCounter++;
-    NSURL *copyFileDirectory = [[NSFileManager.defaultManager temporaryDirectory] URLByAppendingPathComponent:[NSString stringWithFormat:@"%d", appDocId]];
-    if (![NSFileManager.defaultManager createDirectoryAtURL:copyFileDirectory withIntermediateDirectories:YES attributes:nil error:nil]) {
-        LOG_ERR("Could not create directory " << [[copyFileDirectory path] UTF8String]);
+    NSURL *baseDirectory = self.floeEngineCopyDirectory ?: NSFileManager.defaultManager.temporaryDirectory;
+    if (!baseDirectory.isFileURL) {
+        if (errorPtr)
+            *errorPtr = [NSError errorWithDomain:NSCocoaErrorDomain code:NSFileWriteInvalidFileNameError userInfo:nil];
         return NO;
     }
+    NSURL *copyFileDirectory = [baseDirectory URLByAppendingPathComponent:NSUUID.UUID.UUIDString isDirectory:YES];
+    if (![NSFileManager.defaultManager createDirectoryAtURL:copyFileDirectory withIntermediateDirectories:YES attributes:nil error:errorPtr])
+        return NO;
 
-    copyFileURL = [copyFileDirectory URLByAppendingPathComponent:[[[self fileURL] path] lastPathComponent]];
-
-    NSError *error;
-    [[NSFileManager defaultManager] removeItemAtURL:copyFileURL error:nil];
-    [[NSFileManager defaultManager] copyItemAtURL:[self fileURL] toURL:copyFileURL error:&error];
-    if (error != nil)
+    // Never delete a previous editor generation or its recovery data.
+    NSURL *candidate = [copyFileDirectory URLByAppendingPathComponent:self.fileURL.lastPathComponent];
+    if (![NSFileManager.defaultManager copyItemAtURL:self.fileURL toURL:candidate error:errorPtr])
         return NO;
 
     NSURL *url = [[NSBundle mainBundle] URLForResource:@"cool" withExtension:@"html"];
+    if (!url) {
+        if (errorPtr)
+            *errorPtr = [NSError errorWithDomain:NSCocoaErrorDomain code:NSFileReadNoSuchFileError userInfo:nil];
+        return NO;
+    }
+    copyFileURL = candidate;
+    // Opening may fail before resources or files are ready. Allocate the socket
+    // last so a later retry cannot mistake that failed open for a live document.
+    fakeClientFd = fakeSocketSocket();
+    if (fakeClientFd < 0)
+        return NO;
+
     NSURLComponents *components = [NSURLComponents componentsWithURL:url resolvingAgainstBaseURL:NO];
     DocumentData::allocate(appDocId).coDocument = self;
     components.queryItems = @[ [NSURLQueryItem queryItemWithName:@"file_path" value:[copyFileURL absoluteString]],
--- a/ios/ios.h
+++ b/ios/ios.h
@@ -16,6 +16,9 @@
 
 extern int coolwsd_server_socket_fd;
 
+// Set once before starting COOLWSD; invoked after its accept/web polls start.
+extern void (*floeOfficeServerReadyCallback)();
+
 extern COKit *lo_kit;
 
 /* Install the engine-driven clipboard provider: the engine advertises formats on copy and reads
--- a/ios/ios.mm
+++ b/ios/ios.mm
@@ -25,6 +25,7 @@
 const char *user_name = nullptr;
 
 int coolwsd_server_socket_fd = -1;
+void (*floeOfficeServerReadyCallback)() = nullptr;
 
 COKit *lo_kit;
 
--- a/wsd/COOLWSD.cpp
+++ b/wsd/COOLWSD.cpp
@@ -3680,6 +3680,11 @@
 
     WebServerPoll->startThread();
 
+#if defined(IOS)
+    if (floeOfficeServerReadyCallback)
+        floeOfficeServerReadyCallback();
+#endif
+
 #if !MOBILEAPP
     _admin.start();
 #endif
--- a/wsd/ClientSession.cpp
+++ b/wsd/ClientSession.cpp
@@ -27,6 +27,9 @@
 #include <common/JsonUtil.hpp>
 #if defined(QTAPP) || defined(MACOSAPP)
 #include <common/SettingsStorage.hpp>
+#endif
+#if defined(IOS)
+#include <common/MobileApp.hpp>
 #endif
 #include <common/NumUtil.hpp>
 #include <common/Log.hpp>
@@ -69,6 +72,21 @@
 #include <string>
 #include <string_view>
 #include <unordered_map>
+
+#if defined(IOS)
+static void floeRejectSaveRequest(const std::shared_ptr<DocumentBroker>& broker,
+                                  const std::string& requestID)
+{
+    if (requestID.size() > 128 || requestID.rfind("floe-save:", 0) != 0)
+        return;
+    CODocument *document = DocumentData::getDocument(broker->floeMobileAppDocId());
+    NSString *nativeID = [NSString stringWithUTF8String:requestID.c_str()] ?: @"";
+    dispatch_async(dispatch_get_main_queue(), ^{
+        if (document.floeSaveRequestRejected)
+            document.floeSaveRequestRejected(nativeID);
+    });
+}
+#endif
 
 using namespace COOLProtocol;
 
@@ -1020,7 +1038,11 @@
         }
         return sendCombinedTiles(buffer, length, tokens, docBroker);
     }
-    else if (tokens.equals(0, "save"))
+    else if (tokens.equals(0, "save")
+#if defined(IOS)
+             || (tokens.equals(0, "uno") && tokens.equals(1, ".uno:Save"))
+#endif
+             )
     {
         // If we can't write to Storage, there is no point in saving.
         if (!isWritable())
@@ -1028,12 +1050,26 @@
             LOG_WRN("Session [" << getId() << "] on document [" << docBroker->getDocKey()
                                 << "] has no write permissions in Storage and cannot save.");
             sendTextFrameAndLogError("error: cmd=save kind=savefailed");
+#if defined(IOS)
+            std::string requestID;
+            getTokenString(tokens, "extendedData", requestID);
+            floeRejectSaveRequest(docBroker, Uri::decode(requestID));
+#endif
         }
         else
         {
             // Don't save unmodified docs by default.
             int dontSaveIfUnmodified = 1;
             int dontTerminateEdit = 1;
+#if defined(IOS)
+            // Every frontend Save must go through broker admission. Otherwise
+            // an untracked UNO save could steal another request's receipt.
+            if (tokens.equals(0, "uno"))
+            {
+                dontSaveIfUnmodified = 0;
+                dontTerminateEdit = 0;
+            }
+#endif
             std::string extendedData;
 
             // We expect at most 3 arguments.
@@ -1056,6 +1092,9 @@
 
             if (isLockedByPassword())
             {
+#if defined(IOS)
+                floeRejectSaveRequest(docBroker, extendedData);
+#endif
                 // This view has no changes of its own, so the save is answered like a save of
                 // an unmodified document.
                 LOG_INF("Session [" << getId() << "] on document [" << docBroker->getDocKey()
@@ -1067,8 +1106,14 @@
             }
             else
             {
-                docBroker->manualSave(client_from_this(), dontTerminateEdit != 0,
+                const bool accepted = docBroker->manualSave(client_from_this(), dontTerminateEdit != 0,
                                       dontSaveIfUnmodified != 0, extendedData);
+#if defined(IOS)
+                if (!accepted)
+                    floeRejectSaveRequest(docBroker, extendedData);
+#else
+                (void)accepted;
+#endif
             }
         }
     }
@@ -3011,6 +3056,25 @@
                     const bool isAutosave = docBroker->isNextSaveAutosave();
                     object->set("isAutosave", isAutosave);
 
+#if defined(IOS)
+                    // Capture request identity before handleSaveResponse resets it.
+                    // Join it with the native persistence callback by engine result
+                    // sequence, never by callback arrival order or "next save".
+                    std::string sequence;
+                    JsonUtil::findJSONValue(object, "floeSaveSequence", sequence);
+                    const std::string request = docBroker->floeActiveSaveRequestID();
+                    if (!sequence.empty() && sequence.size() <= 32)
+                    {
+                        CODocument *document = DocumentData::getDocument(docBroker->floeMobileAppDocId());
+                        NSString *nativeSequence = [NSString stringWithUTF8String:sequence.c_str()];
+                        NSString *nativeRequest = request.size() <= 128 && request.rfind("floe-save:", 0) == 0
+                            ? ([NSString stringWithUTF8String:request.c_str()] ?: @"") : @"";
+                        dispatch_async(dispatch_get_main_queue(), ^{
+                            if (document.floeSaveSequenceAssociation)
+                                document.floeSaveSequenceAssociation(nativeSequence, nativeRequest);
+                        });
+                    }
+#endif
                     // Save to Storage and log result.
                     docBroker->handleSaveResponse(client_from_this(), object);
 
--- a/wsd/DocumentBroker.hpp
+++ b/wsd/DocumentBroker.hpp
@@ -361,6 +361,14 @@
 
     /// Get whether the next save operation is an autosave.
     bool isNextSaveAutosave() const;
+#ifdef IOS
+    unsigned floeMobileAppDocId() const { return getMobileAppDocId(); }
+    std::string floeActiveSaveRequestID() const
+    {
+        return _docState.activity() == DocumentState::Activity::Save
+            ? _nextStorageAttrs.getExtendedData() : std::string();
+    }
+#endif
 
     /// Handle the save response from Core and upload to storage as necessary.
     /// Also notifies clients of the result.
--- a/common/MobileApp.hpp
+++ b/common/MobileApp.hpp
@@ -57,6 +57,10 @@
     static DocumentData *getIfExists(unsigned docId);
     static void deallocate(unsigned docId);
     static int count();
+#ifdef IOS
+    // Retain the Objective-C document while holding the registry lock.
+    static CODocument *getDocument(unsigned docId);
+#endif
 
 #ifdef IOS
     CODocument *coDocument;
--- a/common/MobileApp.cpp
+++ b/common/MobileApp.cpp
@@ -54,22 +54,35 @@
     return it != idToDocDataMap.end() ? it->second : nullptr;
 }
 
+#ifdef IOS
+CODocument *DocumentData::getDocument(unsigned docId)
+{
+    const std::lock_guard<std::mutex> lock(idToDocDataMapMutex);
+    auto it = idToDocDataMap.find(docId);
+    CODocument *document = it == idToDocDataMap.end() ? nil : it->second->coDocument;
+    return document;
+}
+#endif
+
 void DocumentData::deallocate(unsigned docId)
 {
-    if (idToDocDataMap.find(docId) == idToDocDataMap.end())
+    DocumentData *removed = nullptr;
     {
-        // FIXME: Something is wrong, whatever.
-        return;
+        const std::lock_guard<std::mutex> lock(idToDocDataMapMutex);
+        auto it = idToDocDataMap.find(docId);
+        if (it == idToDocDataMap.end())
+            return;
+        removed = it->second;
+        idToDocDataMap.erase(it);
     }
-    // Does get() really need to called during the destructor?
-    get(docId);
-    auto p = idToDocDataMap.find(docId);
-    delete p->second;
-    idToDocDataMap.erase(docId);
+    // Releasing the retained UIDocument can re-enter host code; do it outside
+    // the registry lock after new lookups can no longer find this generation.
+    delete removed;
 }
 
 int DocumentData::count()
 {
+    const std::lock_guard<std::mutex> lock(idToDocDataMapMutex);
     return idToDocDataMap.size();
 }
 
--- a/wsd/DocumentBroker.cpp
+++ b/wsd/DocumentBroker.cpp
@@ -1170,7 +1170,15 @@
             _isViewFileExtension = COOLWSD::IsViewFileExtension(localStorage->getFileExtension());
             if (session)
             {
-                if (_isViewFileExtension)
+                // Preserve the native embedder's immutable preview permission.
+                // Local files being writable does not authorize this session to
+                // edit, add comments, or switch setviewreadonly back to false.
+                if (Util::isMobileApp() && !session->getWritePermission())
+                {
+                    session->setWritable(false);
+                    session->sendFileMode(true, false, false);
+                }
+                else if (_isViewFileExtension)
                 {
                     LOG_DBG("Setting session [" << sessionId << "] as readonly");
                     session->setReadOnly(true);
