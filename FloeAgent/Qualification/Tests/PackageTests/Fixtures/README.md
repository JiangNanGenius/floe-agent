# Independent OpenPGP fixture

Generated with GnuPG using an ephemeral Ed25519 signing key, solely for qualification.
The temporary private key and keyring were destroyed. `public.asc`, `Release`, and
`InRelease` are public test data and must never be used as a production trust root.
